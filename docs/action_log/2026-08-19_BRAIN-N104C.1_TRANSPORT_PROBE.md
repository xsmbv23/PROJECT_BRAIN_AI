# BRAIN-N104C.1_TRANSPORT_PROBE

## Gate rule

`PASS_IS_LOCAL_TO_GATE` is immutable.

`BRAIN-N104C.1R.RECONCILE = PASS` proves only exact live runtime identity. It does not unlock this transport gate, canonical quorum, database promotion, or Layer 1.

## Independent transport probe

Source:

`https://ketqua16.net/`

The probe is isolated in `tools/transport_probe.py`.

It performs only:

1. HTTP GET
2. require HTTP 200
3. bounded read, maximum 128 KiB
4. SHA-256 of the bounded raw payload
5. byte count
6. truncation detection
7. compact transport receipt

It does **not**:

- parse lottery results
- remove advertisements
- infer truth
- modify source data
- promote the source
- write bulk data into Brain memory

## Current evidence

The public source is reachable and exposes the XSMB traditional result area and other page content, including navigation/advertising-related material. That observation is web evidence only; it is NOT the Render transport receipt. citeturn0view0

Therefore the gate remains:

```text
BOOT IDENTITY RECONCILE = PASS
TRANSPORT PROBE          = LOCKED/PENDING EXACT RUNTIME EXECUTION
CANONICAL QUORUM         = LOCKED
SOURCE QUORUM            = DENY
LAYER 1                  = LOCKED
STAIRCASE                = LOCKED
```

## Important advertising rule

Advertising is transport payload, not permission to mutate the raw source. The Brain/Data boundary must preserve the raw response first. Any later ad/noise classification belongs to a downstream derived layer and cannot alter the raw forensic envelope.

## Memory/OOM rule

The probe is bounded at 128 KiB and performs no bulk DOM/object construction. This is intentionally far below the 320 MiB Render guard.

## Next action

`BRAIN-N104C.1_TRANSPORT_EXEC` — execute this exact probe inside the exact Render runtime `2d4415a875df3582aa26df4598f4f409c3c23027`, capture the independent transport receipt, and compare status/byte count/SHA only. No cascade unlock is permitted.
