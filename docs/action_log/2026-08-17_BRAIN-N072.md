# BRAIN-N072 — Immutable Handoff to Layer 1

## Trigger

Successor architecture review identified a critical semantic clarification:
there is **one** Forensic database admission FSM, not two independent
Forensic systems. Database existence, binding, secret resolution, TLS,
network-origin proof, round-trip, and promotion are sequential gates in the
same chain.

## Rules frozen

```text
PASS IS PREREQUISITE ONLY
NO PASS INHERITANCE
UNKNOWN IS NOT PASS
DEFAULT DENY
FIRST FAIL/UNKNOWN STOPS REACHABILITY
LATER GATES = UNREACHED
FAILURE HISTORY IS IMMUTABLE
```

The chain is:

```text
DB_EXISTENCE
 -> DB_BINDING
 -> SECRET_RESOLUTION
 -> DB_TLS_ADMISSION
 -> NETWORK_ORIGIN_PROOF
 -> DB_ROUND_TRIP
 -> PROMOTION
```

Only a real compact metadata write/read with SHA-256 match can satisfy the
round-trip gate. The Render runtime has not proven the binding, so the
foundation terminal decision remains DENY.

## Layer separation

`Project_Brain_AI` remains frozen as Governance Control Plane.
`xsmbv23/Quant_Engine` is the active Layer 1 execution repository.

Mandatory rule:

> Room in Quant Engine is a function boundary, not a second security boundary.

Quant Engine must not recreate Brain's corridor locks, permission graph,
forensic FSM, or governance layer.

## QUANT-N001 gate

The first execution frame must be minimal:

```text
CANONICAL DATA
  -> INPUT ADAPTER
  -> ROOM 01 / SIGNAL FUNCTION
  -> SCORING
  -> OUTPUT
```

Success is measured as:

```text
RUN -> OUTPUT -> MEASURABLE RESULT
```

No multi-room orchestration or governance duplication before the first real
end-to-end result exists.

## State

```text
BRAIN FOUNDATION = FROZEN
N071 TERMINAL DENY = IMMUTABLE
LAYER 1 = READY
STAIRCASE = LOCKED
NEXT = QUANT-N001
```

## Successor instruction

A future Bot must read this document and
`docs/architecture/FORENSIC_ADMISSION_CHAIN.md` before modifying either Brain
or Quant Engine. The action log remains the execution history; it must be
appended/extended, never rewritten to erase a prior decision.
