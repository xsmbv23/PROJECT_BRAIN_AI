# BRAIN-N087 — Forensic Database Admission Chain Canonization

## Action class

NON-MUTATING FOUNDATION DOCUMENTATION

## Trigger

Successor-safe clarification: the Database Admission states are one Forensic FSM, not multiple independent forensic systems. The rule `PASS_AT_GATE_IS_PREREQUISITE_ONLY; NEVER_INFER_DEEPER_PASS` is now explicitly canonicalized.

## Evidence basis

Exact-current repository state records:

- foundation = FROZEN
- FSM state = WAIT_EXTERNAL_EVENT
- action space = 0
- action = MANDATORY_NO_OP
- promotion = DENY
- Layer 1 = LOCKED
- staircase = LOCKED
- DB binding = BOUND_TLS
- DB TLS admission = PASS
- network origin proof = NOT_PROVEN
- durable DB round-trip = NOT_PROVEN
- next action = `BRAIN-N086_WAIT_NETWORK_ORIGIN_PROOF`

The exact-current runtime anchor is commit `eae5fc2c09c54cc2b13902cdf8d92843d4ca0097`, deploy `dep-da22s4lbedkc73d89kq0`.

## Canonical semantics

```text
ONE FORENSIC DATABASE ADMISSION CHAIN / ONE FSM

DB_EXISTENCE
 -> DB_BINDING
 -> SECRET_RESOLUTION
 -> DB_TLS_ADMISSION
 -> NETWORK_ORIGIN_PROOF
 -> DB_ROUND_TRIP
 -> PROMOTION
```

Each gate has local evidence. PASS is only a prerequisite for the next gate. PASS never inherits to a deeper gate. UNKNOWN/NOT_PROVEN/FAIL remain DENY.

## Mandatory no-op

Because `NETWORK_ORIGIN_PROOF` is not proven, the system must remain:

```text
WAIT_EXTERNAL_EVENT
ACTION_SPACE = 0
MANDATORY_NO_OP
PROMOTION = DENY
```

This is an active safety state. It is not permission for the Foundation to invent a network-origin event or to perform unrelated downstream work.

## Downstream lock

```text
NETWORK_ORIGIN_PROOF
       ↓
DB_ROUND_TRIP
       ↓
PROMOTION
       ↓
DATA_ADMISSION
       ↓
RESEARCH_ADMISSION
       ↓
EVIDENCE_ANALYSIS
       ↓
REPORTING
```

No downstream gate may be advanced while promotion is DENY.

## Architectural correction

Do not describe `DB_EXISTS` and `DB_BINDING` as two separate Forensic systems. They are two local gates inside the same admission FSM. The same applies to TLS admission, origin proof, round-trip, and promotion.

## Preservation

The canonical architecture document was updated in:

`docs/architecture/FORENSIC_DATABASE_ADMISSION_CHAIN.md`

The repository remains the durable authority for successor Bots. No historical state was overwritten; this is an append-only clarification action.

## Result

```text
N087 = CLOSED
mutation = NONE
promotion = DENY
Layer 1 = LOCKED
staircase = LOCKED
next operational action = BRAIN-N086_WAIT_NETWORK_ORIGIN_PROOF
```
