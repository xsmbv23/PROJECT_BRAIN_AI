# BRAIN-N052 — Render Boot-Gate Evidence / First Runtime Failure

## Runtime evidence acquired

Render successfully deployed the exact commit:

`f251efc5d5954ac1e1bdf67877a485c15fcc7201`

The Brain process executed `tools/runtime_boot_gate.py` before binding the HTTP server.

This is the first genuine runtime execution evidence for the foundation path.

## Failure

The first gate failed at `state_consistency` with:

```text
TypeError: the JSON object must be str, bytes or bytearray, not dict
```

Root cause: `check_state_consistency.py` incorrectly performed two `json.loads()` calls against `state/current_state.json`. The persistent state file is already a JSON object; the second decode was invalid.

## Corrective action

Fixed the verifier to decode `current_state.json` exactly once.

No security gate was weakened. The boot gate correctly denied service startup, proving that the new runtime gate actually protects the Brain process.

## Interpretation

```text
RUNTIME EXECUTION = PROVEN
STATE CONSISTENCY = FAIL (repaired)
PROMOTION = DENY
LAYER 1 = LOCKED
STAIRCASE = LOCKED
```

This is **not** a foundation PASS yet.

## Next

`BRAIN-N053` — deploy the repaired HEAD, verify state-consistency PASS, then continue through foundation and access-path gates; capture the final compact runtime evidence and memory metrics.
