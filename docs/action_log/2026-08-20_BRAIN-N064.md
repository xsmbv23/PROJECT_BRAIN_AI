# BRAIN-N064 — Three-Repository Canonical System Map

## Objective

Prevent successor AI agents from treating the three repositories as one codebase and establish a single canonical repository boundary map.

## Repositories verified

- `xsmbv23/Project_Brain_AI` — Brain / Governance / Forensic Control Plane
- `xsmbv23/xsmb-quant` — XSMB Data / Ingestion / Source Foundation / Runtime
- `xsmbv23/Quant_Engine` — Quant / Calculation / Research Engine

All three are public `main`-branch repositories owned by `xsmbv23`.

## Audit evidence

The data repository currently exposes dedicated state authority, AI action/progress ledgers, data contracts, raw artifact storage, reconciliation, security corridor/capability/audit/self-test, and foundation documentation. This confirms that source-truth and data-foundation responsibilities belong to `xsmb-quant`, not Brain. See `docs/STATE_AUTHORITY_HANDOFF.md`, `docs/AI_ACTION_LOG_V1.md`, `docs/AI_PROGRESS_LEDGER_V1.md`, `data/contracts/README.md`, `storage/raw_artifacts.py`, `data/reconciliation/legacy_reconcile.py`, `security/corridor.py`, and `security/capability.py`.

## Decision

The canonical system map is now stored at:

`docs/SYSTEM_REPOSITORY_MAP_V1.md`

It defines:

- repository authority;
- data flow;
- evidence flow;
- cross-repository security corridor;
- directional layer edges;
- Brain/Data/Quant boundary;
- Render memory boundary;
- forensic database admission chain;
- successor-agent rules.

## Critical forensic invariant recorded

```text
DB_EXISTS
   != DB_BOUND
   != DB_TLS_ADMISSION
   != DB_ROUND_TRIP
   != PROMOTION
```

Each gate requires its own evidence. PASS does not propagate automatically to a later gate. Unknown is not Pass. Default is Deny.

## Foundation status

```text
THREE_REPOSITORY_MAP = ESTABLISHED
DATA_AUTHORITY       = xsmb-quant
CALC_AUTHORITY       = Quant_Engine
GOVERNANCE_AUTHORITY = Project_Brain_AI
PROMOTION            = DENY
LAYER_1              = LOCKED
STAIRCASE            = LOCKED
```

## Next action

`BRAIN-N065` — audit the actual cross-repository communication edges against the canonical map. Identify every caller/callee boundary, capability token, corridor key, room key, protected-room latch, and evidence return path. No edge is considered safe merely because both repositories are correct in isolation.
