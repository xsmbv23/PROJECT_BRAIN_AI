# QUANT-N003 — Replay Truth / Forensic Invariant Hardening

## Scope

This action hardens Layer 1 replay at architecture level. It is explicitly separate from signal/ML/alpha development.

## Changes

The Quant Engine now enforces:

- typed canonical hash domain
- deterministic list/tuple order
- unordered set/frozenset rejection
- explicit float type + fixed precision
- explicit execution signature: room, code hash, Python version, implementation, OS, platform, architecture, dependency hash
- read-only frozen input acquisition with identity metadata verification
- static replay-module external dependency guard
- explicit reproducibility versus correctness distinction

## Invariant

```text
FRESH_1 == REPLAY_1 == REPLAY_2 == FRESH_2
```

## Critical rule

```text
REPRODUCIBLE != CORRECT
```

A replay hash match is evidence of consistency, not evidence that the algorithm or signal is correct.

## Gate

```text
N003 PASS -> QUANT-N004 may be considered
N003 FAIL/UNKNOWN -> QUANT-N004 LOCKED
```

## Successor instruction

Do not add alpha, ML, signals, or trading logic before this invariant contract is proven. Read `docs/architecture/N003_REPLAY_FORENSIC_INVARIANTS.md` in Brain and `contracts/N003_REPLAY_FORENSIC_INVARIANTS.md` in Quant Engine first.
