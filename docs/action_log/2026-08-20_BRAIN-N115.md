# BRAIN-N115 — Exact-Current /governance Receipt Observation

## Decision

`BRAIN-N115` is **NOT PROVEN CURRENT** and remains DENY.

The live Render service was inspected through the Render control plane and its exact-current deployment was identified as:

- service: `project-brain-ai`
- service id: `srv-da0506u1egvs73ftsdng`
- live commit: `e23a5baa645753306a1a829a2ffcf72015a8f07b`
- live deploy: `dep-da38hngu01pc73854hh0`
- live instance observed in runtime logs: `srv-da0506u1egvs73ftsdng-7wgsv`

The exact-current runtime logs contain:

```text
runtime_boot_gate = PASS
foundation = PASS
access_path = PASS
209 tests = PASS
DB_BINDING = BOUND_TLS
DB_TLS_ADMISSION = PASS
DB_ROUND_TRIP = PASS
NETWORK_ORIGIN_PROOF = PASS
PROMOTION = DENY
```

However, the same exact-current boot evidence reports:

```text
admission_gate = DENY
admission_denials = [ACTION_RECEIPT]
action_receipt = DENY / ACTION_RECEIPT_MISSING
```

The requested independent `/governance` observation could not be obtained through the available safe control surfaces during N115. No request receipt for `/governance` was present in Render request logs for the inspected window, and the external execution environment could not resolve the Render hostname.

Therefore N115 cannot mark the prior-runtime action receipt as current proof.

## Important distinction

The database round-trip evidence observed in the Render application log is **not** silently promoted to the action-receipt gate. They are separate gates in the **same single Forensic FSM**.

```text
DB_ROUND_TRIP = PASS
    !=
ACTION_RECEIPT = PASS_LOCAL
```

Likewise:

```text
DB_EXISTENCE = PASS
    != DB_BINDING = PASS
    != SECRET_RESOLUTION = PASS
    != DB_TLS_ADMISSION = PASS
    != NETWORK_ORIGIN_PROOF = PASS
    != DB_ROUND_TRIP = PASS
    != PROMOTION
```

Every gate owns its own evidence.

## No mutation

No credential was exposed.
No source dataset was downloaded.
No historical receipt was overwritten.
No Layer 1 room was unlocked.
No staircase was opened.
No promotion occurred.

## Exact current state

```text
ACTION_RECEIPT       = NOT_YET_PROVEN_CURRENT
PROMOTION            = DENY
ROOM_02              = LOCKED
STAIRCASE            = LOCKED
```

## Next action

`BRAIN-N116_WAIT_EXTERNAL_OBSERVATION` — wait for an independently observable exact-current `/governance` receipt event. The Foundation must not manufacture the event, mutate external state to create it, or treat Render startup logs as equivalent to an independent HTTP observation.
