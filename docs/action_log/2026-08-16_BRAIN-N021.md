# BRAIN-N021 — Executable Brain Core Hardening

## Status
IMPLEMENTED / RUNTIME VERIFICATION PENDING

## Added

`core/foundation_hardening.py`

Compact primitives now exist for:

- policy/version pinning;
- short-lived capability lease validation;
- capability scope, nonce and operation-limit checks;
- circuit breakers with cooldown;
- schema-major fail-closed validation;
- terminal QUARANTINED state;
- deterministic tamper-evident audit hash chain.

## Design rule

These primitives are metadata-only and independent of the chat UI. They do not own canonical data and do not promote evidence.

## Important limitation

Implementation existence is not runtime verification. The next gate must execute these controls against bounded fixtures and verify restart-safe state continuity. Until then: `PROMOTION=DENY`, `LAYER_1=LOCKED`, `STAIRCASE=LOCKED`.

## OOM rule

No bulk data is loaded by this module. Render Free remains 512MB hard ceiling with 320MB conservative guard.

## Next

BRAIN-N022: add compact tests and runtime gate integration for these primitives, including fail-closed cases and audit-chain continuity.
