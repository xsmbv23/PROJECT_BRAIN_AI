# BRAIN-N104C.1R.REPAIR2 — Exact Runtime Identity Repair

## Trigger

The authoritative N104C.1R state requires exact-current Render runtime identity before any transport inspection. Render currently reports the intended commit `de47a5ce136fd1571c121e31011c09c66342c5d6` as LIVE, but the repository health boundary did not expose the exact runtime commit/liveness tuple required by the reconciliation contract.

The action-receipt runtime verifier also contained a schema mismatch: it extracted canonical `last_action_id` / `next_action_id` but passed legacy key names into the canonical validator. That made a valid canonical receipt impossible to validate.

## Repairs implemented

### 1. Runtime identity endpoint

`brain/server.py` now emits, on the existing read-only health boundary:

```text
HTTP 200
status = 200
liveness = LIVE
commit_sha = RENDER_GIT_COMMIT
```

The response remains compact and contains no credentials or source payload.

### 2. Canonical action-receipt validation

`tools/verify_action_receipt_runtime.py` now passes:

```text
last_action_id
next_action_id
```

directly to `validate_action_receipt`.

Legacy aliases are not used to satisfy the canonical validator.

## Git evidence

Health boundary repair commit:

```text
45a19502e7bbad4103e5be4c9601b34168e9c891
```

Action-receipt verifier repair commit:

```text
a772c7b69b21462a19706bf73c8ee6a364bc1fb8
```

These changes are on `main`. Render auto-deploy is enabled for `project-brain-ai`.

## Exact-current rule

The repository changes above are **NOT runtime proof**.

The next authoritative evidence must come from a new Render deployment whose exact commit is the repaired runtime, followed by `/health` response verification.

Until then:

```text
RENDER LIVE                = previous exact deployment only
REPAIRED RUNTIME IDENTITY  = NOT PROVEN
ACTION RECEIPT RECONCILE   = NOT PROVEN
N104C.1 TRANSPORT          = LOCKED
CANONICAL QUORUM           = DENY
TRUTH ADMISSION            = DENY
```

## Forensic invariant

Do not rewrite `current_state.json` merely because the repaired code exists in GitHub. Do not create a receipt for the repair. Do not manufacture a runtime identity. The runtime must prove itself.

## Next action

`BRAIN-N104C.1R` continues unchanged: wait for/observe the exact-current Render deployment, verify `/health` contains HTTP 200 + exact runtime commit + `liveness=LIVE`, then reconcile state and receipt. Only after that may the one-shot transport probe be enabled.
