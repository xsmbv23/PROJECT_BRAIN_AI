# BRAIN-N075 — Successor and Replay Verification of the One-Chain Forensic FSM

## Status

`CLOSED_PASS`

N075 is proven by the exact-current Render runtime, not by local inference.

## Exact runtime receipt

Deployment:

`dep-da20perl550s73av1p10`

Commit:

`1fbda864623fd11ae85a298ced08ae55f912ca9d`

Runtime boot:

```text
PASS
```

Foundation:

```text
78 tests
0 failures
tracemalloc_peak_bytes = 1,142,591
memory_guard_bytes = 335,544,320
```

Access path:

```text
PASS
protected_room = OWNER_ROOM
state = RELEASED
```

Database admission:

```text
DB_EXISTENCE = PREREQUISITE_EXTERNAL_EVIDENCE
DB_BINDING = DENY_TLS
DB_TLS_ADMISSION = DENY
DB_ROUND_TRIP = NOT_PROVEN
PROMOTION = DENY
```

## Deterministic replay receipt

The exact-current runtime replayed the machine-readable admission FSM and produced:

```text
replay = PASS
mutation = NONE
external_event_manufactured = false
external_before == external_after = true
action_space_unchanged = true
foundation_cannot_mutate_external = true
foundation_cannot_unlock_room = true
room_02_unchanged = true
room_02_unlocked = false
staircase_unchanged = true
staircase_unlocked = false
no_pass_inheritance = true
pass_locality = true
unknown_not_pass = true
fail_is_evidence = true
external_noop = true
inner_release = true
EV negative = DENY
EV unknown = DENY
EV NaN = DENY
EV infinite = DENY
EV zero = NOT_SUFFICIENT
```

Canonical FSM hash replay was deterministic:

```text
84c0ad581343556dba75a813401d3b8618b060ed96ab5fc44c8e99f3c773205b
```

Repeated hash:

```text
84c0ad581343556dba75a813401d3b8618b060ed96ab5fc44c8e99f3c773205b
```

## Critical invariant discovered during closure

The durable PostgreSQL adapter previously auto-upgraded an unspecified `sslmode` to `require`.

That behavior was inconsistent with the Forensic admission chain because the admission probe correctly reported `DENY_TLS` for the same binding.

N075 therefore includes a hardening repair:

```text
UNSPECIFIED / UNSAFE TLS
        ↓
DATABASE_TLS_NOT_EXPLICIT
        ↓
DENY
```

The adapter no longer manufactures a stronger security state than the admission FSM observed.

This is a critical Forensic invariant:

> Infrastructure adapters may not upgrade an admission state. They must consume the already-admitted security state or fail closed.

## Successor doctrine

The database chain remains:

```text
DB_EXISTENCE
  ↓
DB_BINDING
  ↓
SECRET_RESOLUTION
  ↓
DB_TLS_ADMISSION
  ↓
NETWORK_ORIGIN_PROOF
  ↓
DB_ROUND_TRIP
  ↓
PROMOTION
```

Every gate requires fresh evidence. PASS is local to the gate. PASS is only a prerequisite for the next gate. No PASS inheritance is permitted.

## Final N075 state

```text
FORENSIC FSM            = VERIFIED
DETERMINISTIC REPLAY    = PASS
EXTERNAL EVENT PATH     = UNCHANGED
ACTION SPACE            = 0
ROOM 02                 = LOCKED
STAIRCASE               = LOCKED
PROMOTION               = DENY
DATABASE TLS            = DENY
DB ROUND TRIP            = NOT PROVEN
```

## Next

`BRAIN-N076` — resolve the external Render Secret binding so the exact-current runtime reports `BOUND_TLS`. No secret may enter GitHub, logs, Brain state, or action receipts. If the external binding remains without explicit TLS, keep durable promotion denied.
