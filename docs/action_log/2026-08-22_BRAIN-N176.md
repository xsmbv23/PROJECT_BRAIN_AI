# BRAIN-N176 — S1 Admission Verifier Hardening

## Intent
Execute the approved upgrade sequence beginning with S1. The objective is to turn the existing S1 canonical-evidence contract into an executable, fail-closed verifier without fabricating admission state.

## Changes
- Added `core/s1_admission.py` as a stdlib-only S1 verifier.
- Verifier enforces real/traceable provenance, authorized acquisition channel, acquisition reference/timestamp, full consecutive-date coverage, zero unresolved conflicts, non-synthetic data, evidence-root path confinement, raw-byte SHA-256 verification, explicit canonical-artifact SHA-256 verification, and observable admission-receipt presence.
- Strengthened `contracts/s1_canonical_evidence_manifest.schema.json` so `canonical_artifact_path` is explicit and hash-verifiable rather than inferred from the raw artifact.
- Added `core/test_s1_admission.py` as a stdlib-only executable self-test covering PASS, partial coverage DENY, synthetic-data DENY, and canonical-hash mismatch DENY.
- Added `.github/workflows/s1-admission-verifier.yml` to run the verifier self-test on relevant changes and verify a supplied/discovered S1 manifest when one exists.

## Commits
- `a71e1fe28f4f7bf07798851530057bc7e0e232d8` — initial S1 verifier
- `9f18bb6be8f0ad4001d8237c57c1c40b67b0eaeb` — explicit canonical artifact contract
- `230e1f9b688f59d62893619241c05a00b2e3ec68` — test import hardening
- `6662c1bc7dccef1a0911c47da8f6eba01ac278e6` — stdlib self-test
- `679829f8dfd8143e0a9e624e0127803c226b7c29` — CI enforcement

## Current forensic status
- This action **does not promote S1**.
- Existing N175 S1 observation remains DENY because lawful acquisition reference, complete coverage, zero conflicts, fresh admission receipt, and frozen canonical SHA-256 are not yet established.
- No canonical state mutation was performed.
- Local/container execution could not reach GitHub due to the execution environment's DNS/network restriction; GitHub-side CI is the authoritative runtime verification path for these changes.

## Next action
Close the real S1 evidence gaps using an authorized/durable acquisition channel, produce the explicit raw and canonical artifacts plus admission receipt, then run the new verifier against that exact manifest. Only after an exact-current S1 PASS should worker-contract hardening begin.
