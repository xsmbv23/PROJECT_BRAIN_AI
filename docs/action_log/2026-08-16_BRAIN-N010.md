# BRAIN-N010 — Real Source Fixture Established

## Source discovery

A persistent Library artifact was found:

- `Ket_Qua_Loto27.xlsx`
- size: 425032 bytes
- source SHA-256: `96328e7808165f60fd4513f2dbe77936c12e3fc3f918896c62e3f0049306e225`

The artifact is a real source artifact already present in the persistent project context. It is not fabricated by this action.

## Bounded extraction

A single row was extracted for `12/08/2026`.

The row contains exactly 27 prize tokens with semantic lengths:

```text
10 x 5 digits
10 x 4 digits
3  x 3 digits
4  x 2 digits
```

This matches the established XSMB 27-prize structure.

## FULL_27 / TAIL_27 boundary

The full prize strings were preserved in the fixture.

TAIL_27 was derived mechanically from the final two digits of each FULL_27 token.

No reverse reconstruction was performed.

## Quorum

The artifact has `source_count=1`.

Foundation canonicalization requires quorum >= 2 independent domains.

Therefore:

```text
STRUCTURAL VALIDATION = PASS
TAIL DERIVATION       = PASS
QUORUM                 = FAIL
CANONICAL              = DENY
PROMOTION             = DENY
```

This is intentional and is a successful fail-closed outcome, not an error.

## Artifacts committed

In `xsmb-quant`:

- `fixtures/2026-08-12/full27_fixture.json`
- `fixtures/2026-08-12/MANIFEST.json`

The fixture is explicitly marked `VERIFICATION_ONLY` and cannot satisfy production evidence gates.

## Next action

Run the bounded parser/derivation/evidence chain against this fixture without loading the historical dataset. Capture runtime evidence, then test the explicit quorum-deny path.

Render 512MB hard boundary remains active.
Layer 1 remains locked.
