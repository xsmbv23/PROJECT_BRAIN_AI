# BRAIN-N034 — CI Verification Wiring

## Action

Replaced the previous split CI checks with the single deterministic foundation verifier:

`python tools/verify_foundation.py`

This ensures successor Bots cannot accidentally run only a subset of the foundation checks.

## Included checks

- complete unittest suite;
- foundation gate;
- credential/source scan;
- core adapter import scan;
- durable audit round-trip;
- explicit room-lock/layer-edge verification;
- bounded memory measurement.

## Execution observation

The commit was pushed successfully. The GitHub connector did not yet expose a workflow run for the commit at verification time, so CI is recorded as `PENDING`, not PASS.

## Critical architecture correction

N033 corrected room-layer semantics from same-layer-only to explicit directional layer edges. This preserves the building model: a stair/corridor may connect layers, but only when that edge is explicitly authorized by the destination room's lock.

## State integrity

`state/current_state.json` was reconciled because it had fallen behind `state/next_action.json`. This stale-state condition is itself treated as a successor-Bot hazard.

## Promotion

Still DENY. Layer 1 LOCKED. Staircase LOCKED.
