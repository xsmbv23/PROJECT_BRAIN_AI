# BRAIN-N031 — Per-Room Lock Integration

## Correction

The communication-security model is now elevated from documentation to a mandatory verification primitive.

A Brain room is not merely a logical module. It is an isolated security boundary with its own capability and key fingerprint.

## Invariants

```text
KEY_A -> ROOM_A only
KEY_B -> ROOM_B only

ROOM_A capability != ROOM_B capability

valid key != valid corridor
valid corridor != valid layer
```

All dimensions must match.

## Added to deterministic verifier

`tools/verify_foundation.py` now checks a valid room entry and an explicit cross-layer denial.

## Required behavior

Any mismatch produces a fail-closed `GovernanceDeny`.

No master key exists in Brain core.

## Status

IMPLEMENTED / EXECUTION PENDING.

Foundation Closure must not be issued until room-lock tests execute successfully together with all previous foundation controls.

## Next

BRAIN-N032: execute complete verifier, then build the Foundation Closure Audit / immutable architectural map if and only if all gates pass.
