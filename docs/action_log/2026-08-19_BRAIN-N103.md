# BRAIN-N103 — Independence Probe Gate

## Rule locked

Each Gate proves exactly one proposition. A Gate PASS cannot be inherited by another Gate unless the downstream Gate independently proves its own proposition.

For source independence, hostname inequality is not sufficient evidence. Independence requires fresh evidence across at least:

1. resolved network identity,
2. network ownership / ASN boundary,
3. absence of a common infrastructure dependency strong enough to constitute a single point of failure.

## Fresh execution

UTC: `2026-08-19T07:47:32.666074+00:00`

Targets:

- `ketqua16.net`
- `xsmb.com.vn`

Observed from the execution environment:

```text
ketqua16.net -> no DNS resolution obtained
xsmb.com.vn  -> no DNS resolution obtained
RDAP          -> unavailable for both targets
```

## Decision

```text
independence_proven = false
status              = DENY_INDEPENDENCE
canonical_quorum    = LOCKED
N104                = NOT OPEN
```

## Forensic reasoning

This is **not** proof that the two sources share infrastructure.

It is also **not** proof that they are independent.

It is an UNKNOWN state at the evidence layer, therefore the admission rule converts UNKNOWN to DENY.

No ASN, organization, proxy, CDN, IP ownership, or network relationship was inferred from hostname names.

## Successor instruction

A later Bot must not replace this DENY with PASS by assumption or by reusing stale DNS/RDAP observations. N103 must be rerun with fresh evidence from an execution environment capable of obtaining authoritative DNS/RDAP data, and the complete receipt must be persisted before N104 can be considered.
