# BRAIN-N086 — External Network-Origin Proof Wait

## Exact-current boundary

N085 proved the exact-current deployment and closed the verification action. The current foundation state is now waiting on a real external infrastructure event.

Render service configuration currently reports:

```text
service = project-brain-ai
service_id = srv-da0506u1egvs73ftsdng
plan = free
cpu = 1 instance
region = oregon
autoDeploy = yes
branch = main
```

The Render service IP allowlist is already:

```text
0.0.0.0/0
```

Therefore no additional IP-allowlist mutation is justified by the current evidence.

## Exact-current runtime evidence from N085

```text
commit = eae5fc2c09c54cc2b13902cdf8d92843d4ca0097
deploy = dep-da22s4lbedkc73d89kq0
status = live
runtime_boot_gate = PASS
state_consistency = PASS
foundation = PASS
access_path = PASS
tests = 93
tracemalloc_peak_bytes = 14024345
memory_guard_bytes = 335544320
database_binding = BOUND_TLS
database_tls_admission = PASS
network_origin_proof = NOT_PROVEN
network_reason = NETWORK_ORIGIN_PROBE_FAILED:OperationalError
network_admission_probe = DISABLED
db_round_trip_action = DENY
write_read_hash_match = false
promotion = DENY
mutation = NONE
```

## Forensic interpretation

The open IP allowlist does not constitute proof that the exact-current Render runtime can reach the declared Singapore database origin.

Likewise:

```text
BOUND_TLS
    != NETWORK_ORIGIN_PROOF

NETWORK_ORIGIN_PROOF
    != DB_ROUND_TRIP

DB_ROUND_TRIP
    != PROMOTION
```

Each gate remains local. No PASS is inherited downstream.

## Mandatory no-op

```text
ACTION_SPACE = 0
MODE = MANDATORY_NO_OP
PROMOTION = HARD_DENY
LAYER_1 = LOCKED
STAIRCASE = LOCKED
```

No self-manufactured network event is permitted.

## Allowed work while waiting

- inspect exact-current runtime evidence;
- inspect Render service/database topology without mutating it;
- append non-mutating forensic documentation;
- verify documentation and state-artifact integrity;
- preserve OOM guard and one-instance boundary.

## Forbidden

- fabricate network reachability;
- convert `OperationalError` into PASS;
- widen or otherwise mutate network policy without evidence that the mutation is required;
- expose or copy credentials;
- perform a durable DB write/read test before network-origin proof;
- unlock Layer 1;
- unlock the staircase;
- advance research/reporting authority.

## Next event

Remain at `BRAIN-N086_WAIT_NETWORK_ORIGIN_PROOF` until a fresh exact-current runtime observation proves the Oregon Render runtime can reach the declared Singapore PostgreSQL origin.
