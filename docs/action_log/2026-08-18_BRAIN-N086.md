# BRAIN-N086 — External Network-Origin Proof Wait

## Exact-current boundary

N085 proved the exact-current deployment and closed the verification action. The current foundation state is waiting on a real external infrastructure event.

This wait is an **active safety state**, not inactivity:

```text
ACTION_SPACE = 0
MODE = MANDATORY_NO_OP
```

## Exact-current runtime evidence from N085

```text
commit = eae5fc2c09c54cc2b13902cdf8d92843d4ca0097
deploy = dep-da22s4lbedkc73d89kq0
status = live
runtime_boot_gate = PASS
state_consistency = PASS
foundation = PASS
access_path = PASS
tests = 93
tracemalloc_peak_bytes = 14024345
memory_guard_bytes = 335544320
database_binding = BOUND_TLS
database_tls_admission = PASS
network_origin_proof = NOT_PROVEN
network_reason = NETWORK_ORIGIN_PROBE_FAILED:OperationalError
network_admission_probe = DISABLED
db_round_trip_action = DENY
write_read_hash_match = false
promotion = DENY
mutation = NONE
```

## One Forensic FSM

The database states are one ordered admission chain:

```text
DB_EXISTENCE
  -> DB_BINDING
  -> DB_TLS_ADMISSION
  -> NETWORK_ORIGIN_PROOF
  -> DB_ROUND_TRIP
  -> PROMOTION
```

`PASS` is local to the gate that produced the evidence. It is a prerequisite for the next evaluation, never an inherited permission.

```text
BOUND_TLS
    != NETWORK_ORIGIN_PROOF

NETWORK_ORIGIN_PROOF
    != DB_ROUND_TRIP

DB_ROUND_TRIP
    != PROMOTION
```

Each transition requires fresh, observable evidence from the exact-current runtime.

## Network blocker

The Render service is in Oregon and the declared PostgreSQL resource is in Singapore. The service IP allowlist is already `0.0.0.0/0`.

An open IP allowlist is **not** network-origin proof. The current `OperationalError` remains an unresolved infrastructure boundary condition.

No hypothesis about private networking, DNS, hostname selection, or external URL is promoted to fact without exact-current evidence.

## Mandatory no-op

```text
PROMOTION = HARD_DENY
LAYER_1 = LOCKED
STAIRCASE = LOCKED
```

No self-manufactured external event is permitted.

## Allowed while waiting

- inspect exact-current runtime evidence;
- inspect Render service/database topology without mutating it;
- append non-mutating forensic documentation;
- verify documentation and state-artifact integrity;
- preserve the OOM guard and one-instance boundary.

## Forbidden

- fabricate network reachability;
- convert `OperationalError` into PASS;
- treat IP allowlisting as connectivity proof;
- treat `BOUND_TLS` as reachability proof;
- perform durable DB write/read testing before network-origin proof;
- expose or copy credentials;
- unlock Layer 1;
- unlock the staircase;
- advance research/reporting authority.

## Successor handoff

Every successor Bot must read:

- `state/current_state.json`
- `state/next_action.json`
- `docs/FORENSIC_DATABASE_ADMISSION_CHAIN.md`
- this action log

before touching the database admission path.

The next permitted operational transition is only after a fresh exact-current runtime observation proves the Oregon Render runtime can reach the declared Singapore PostgreSQL origin.
