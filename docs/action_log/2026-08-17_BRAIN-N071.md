# BRAIN-N071 — Final Foundation Admission Decision

## Decision

The Forensic database admission foundation is now **FROZEN**.

The exact Render service configuration was inspected. The service is a Python Free web service with one instance, auto-deploy on `main`, and the exact runtime anchor policy remains mandatory.

The safe control surface available to this agent can inspect service metadata and deployment state, but it does not expose a safe mutation that can bind the Render PostgreSQL credential into the service Secret Environment without receiving the credential value from an external human-controlled boundary.

The system must not fabricate, infer, copy, expose, or store that credential.

Therefore N071 records an **IMMUTABLE EXTERNAL-LIMITATION DENY**.

## Admission chain status

```text
DB_EXISTENCE       = PASS
DB_BINDING         = NOT_BOUND
SECRET_RESOLUTION  = UNREACHED
DB_TLS_ADMISSION   = UNREACHED
NETWORK_ORIGIN     = UNREACHED
DB_ROUND_TRIP      = UNREACHED
PROMOTION          = DENY
```

This is a valid terminal foundation outcome under the N071 completion gate. It is not a runtime failure and it is not evidence that PostgreSQL is broken.

## Forensic rule preserved

```text
DB_EXISTS != DB_ACCESS
DB_ACCESS != SECRET_RESOLUTION
SECRET_RESOLUTION != TLS_ADMISSION
TLS_ADMISSION != NETWORK_ORIGIN
NETWORK_ORIGIN != DURABLE_ROUND_TRIP
```

No PASS is inherited.

The database admission chain remains one Forensic FSM, not multiple Forensic systems.

## Historical integrity

Previous events are not rewritten. N071 appends the terminal decision.

The external limitation is:

> Render service configuration can be inspected safely, but the available mutation boundary does not provide a credential-free way for this agent to establish `DATABASE_URL` as a Render Secret Environment value.

Required external action, if durable PostgreSQL evidence is later needed:

1. An authorized operator establishes `DATABASE_URL` in Render Secret Environment.
2. No credential is copied into GitHub, Brain source, logs, chat, or action receipts.
3. The service is redeployed.
4. The exact runtime anchor reports non-secret `SECRET_RESOLUTION` and `BOUND_TLS` evidence.
5. Network-origin proof is established.
6. The temporal nonce A/B compact metadata write/read SHA-256 match is executed.
7. Only then may durable evidence promotion be considered.

## Foundation freeze

```text
FORENSIC FOUNDATION = FROZEN
BRAIN CORE           = FROZEN
DATABASE GATES       = FROZEN
LAYER 1              = READY FOR SEPARATE QUANT ENGINE REPOSITORY
STAIRCASE            = LOCKED UNTIL LAYER 1 ADMISSION
```

## Successor instruction

The next Bot must **not reopen infrastructure work merely because DB promotion is DENY**. N071 is a valid terminal foundation decision. Continue in `xsmbv23/Quant_Engine` under the frozen contracts.

The database binding limitation remains a prerequisite if a future component genuinely requires durable Brain evidence.
