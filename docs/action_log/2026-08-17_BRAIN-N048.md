# BRAIN-N048 — Execution Evidence Path

## Diagnosis

The repository is healthy and public, default branch `main`, with push/PR foundation workflow configured. The workflow runs the integrated foundation verifier and the focused protected-room verifier.

GitHub connector observation still returns no workflow runs/status for the latest commits. Therefore execution state remains **UNKNOWN**. No PASS is inferred from workflow configuration.

## Corrective action

Created `docs/FOUNDATION_EXECUTION_PROTOCOL.md` defining the canonical local execution and evidence envelope for successor Bots.

Required evidence:

- exact commit SHA;
- exact command(s);
- executor identity;
- UTC start/end timestamps;
- exit code;
- compact stdout/stderr;
- tracemalloc peak;
- verifier/version fingerprint;
- source-tree fingerprint.

## Canonical commands

```bash
python tools/verify_foundation.py
python tools/verify_access_path.py
```

or:

```bash
./tools/verify_foundation.sh
```

## Security interpretation

No verifier result is promoted from one gate to another automatically. Brain remains governance-only. CapabilityLease remains distinct from corridor key, room key and inner release.

## OOM interpretation

Verifier remains dataset-free and stdlib-only. Render Free 512MB / 1 CPU remains a hard boundary; 320 MiB remains the conservative memory gate.

## State

Promotion DENY. Layer 1 LOCKED. Staircase LOCKED.

Next: BRAIN-N049 — execute the canonical protocol with reproducible evidence, or diagnose the remaining execution environment if the current runtime cannot execute repository code.
