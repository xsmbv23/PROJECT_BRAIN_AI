# BRAIN-N066 — N003 Proof-Hardening Implementation Handoff

## Objective

Synchronize Brain's persistent memory with the actual Layer 1 Quant_Engine proof hardening.

## Quant_Engine changes

Authoritative commits:

```text
forensic_contract.py
commit = a0e7678265342f3147704cc89a55fd7d50d479e1
```

Added exact raw-byte SHA-256 and semantic execution trace hashing.

```text
replay.py
initial trace commit = 9f0936b621b728a3eee1c69669a6e38cd5866fde
corrected replay commit = 39cde1bd351ca908d9abc833b6f4b222f09bff68
```

The correction is important: `CandidateSignalV4` has no `selected` field. The `VALID_EMPTY` reason logic was therefore corrected to use the actual Room 01 semantics:

```text
no features                         -> INSUFFICIENT_DATA
all features recency-excluded       -> FILTERED_OUT
features exist but output is empty  -> NO_SIGNAL
```

This avoids a false forensic classification caused by an invented field.

```text
tests/test_n003_forensic_invariants.py
commit = 6b491ddb1e48521a188bb7f790ed6b4c816eb9c7
```

The invariant tests cover raw-byte line-ending identity and semantic trace ordering/runtime-field rejection.

## Important limitation

These are **proof-harness upgrades**, not proof completion.

The system has NOT yet demonstrated:

```text
MULTI-SCENARIO FRESH = REPLAY
+ TRACE EQUIVALENCE
+ MUTATION/ANTI-CHEAT SURVIVAL
```

Therefore:

```text
N003 = IMPLEMENTED_NOT_PROVEN
N004 = LOCKED
LAYER_2 = NOT_ALLOWED
PROMOTION = DENY
```

## Doctrine preserved

```text
SAME RESULT != SAME EXECUTION PATH
FORENSIC REPRODUCIBILITY != PREDICTIVE EDGE
PASS_AT_GATE_IS_PREREQUISITE_ONLY
UNKNOWN_IS_NOT_PASS
DEFAULT_DENY
```

## Next action

`QUANT-N003-PROOF` — execute the real persisted Room 01 V4 replay across multiple scenarios and attempt to break it with mutation/anti-cheat cases. Do not weaken any invariant to obtain PASS.
