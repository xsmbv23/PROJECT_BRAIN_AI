# BRAIN-N004 — Render Verification Boundary

## Purpose

Close the gap between Brain security verification and actual deployed runtime verification.

## What was discovered

The existing Render service is:

```text
project-brain-ai
repo: xsmbv23/PROJECT_BRAIN_AI
branch: main
autoDeploy: yes
runtime: python
region: oregon
plan: free
```

The service was previously live on an older commit. A manual deploy was therefore triggered against the current main branch.

## Deployment flow

```text
GitHub main
   │
   ▼
Render clone
   │
   ▼
checkout current commit
   │
   ▼
pip install -r requirements.txt
   │
   ▼
python brain_server.py
   │
   ▼
port 10000
   │
   ▼
/health /governance read boundary
```

## Runtime hardening

Render health probes previously produced:

```text
HEAD / → 501
```

The canonical Brain server was hardened to support `HEAD` on `/`, `/health`, and `/governance` without creating any mutation path.

New server commit:

```text
`d298f354780ce4539291cb41bcba7871b2806d73`
```

## Security boundary

The runtime response remains:

```text
promotion = DENY
mutation   = DENY
layer_1    = LOCKED
evidence   = COMPACT_ENVELOPE_ONLY
```

No database access was added. No crawler was added. No promotion endpoint was added.

## Verification state

Render deployment of the server hardening is in progress. Therefore this action does NOT declare Render runtime PASS yet.

## Flow for completion

```text
Deploy
  ↓
Runtime live
  ↓
GET /health
  ↓
GET /governance
  ↓
assert promotion=DENY
assert mutation=DENY
assert layer_1=LOCKED
assert evidence=COMPACT_ENVELOPE_ONLY
  ↓
record immutable evidence
  ↓
FOUNDATION RENDER GATE = PASS
```

## Next action

`BRAIN-N005`: verify the new Render deployment and endpoint semantics, then create the first explicit XSMB Data ↔ Brain corridor contract and its lineage/evidence schema. Layer 1 remains locked.
