# BRAIN-N160 — One-FSM Gate Interaction + Parallel E2E Continuation

## Decision

The database admission chain is explicitly one Forensic FSM, not multiple independent Forensic state machines.

`PASS` is local evidence only. It makes the next gate reachable; it never inherits PASS to the next gate.

```text
DB_EXISTENCE
 -> DB_BINDING
 -> SECRET_RESOLUTION
 -> DB_TLS_ADMISSION
 -> NETWORK_ORIGIN_PROOF
 -> DB_ROUND_TRIP
 -> PROMOTION
```

Each gate owns its own evidence. FAIL/UNKNOWN stops reachability; later gates become UNREACHED.

## Persistent successor artifact

`docs/architecture/FORENSIC_GATE_INTERACTION.md`

## Parallel policy

Brain remains externally blocked at `BRAIN-N125_WAIT_EXTERNAL` with `action_space=0` and `promotion=DENY`. This action therefore does not unlock Brain, Room 02, or the staircase.

Safe parallel work continues only in Quant_Engine as a local prerequisite for S2 VALID_RESEARCH. Quant Engine received `contracts/forensic_gate_semantics_v1.json` so the same non-inheritance semantics are enforced at the Layer 1 adapter boundary.

## E2E position

```text
S1 REAL_DATA                 = admitted foundation boundary
S2 VALID_RESEARCH            = current segment
S3 VALID_BACKTEST             = blocked
S4 EDGE                       = blocked
S5 EV_PNL_ROI                 = blocked
S6 ROBUSTNESS_RISK_DRIFT     = blocked
S7 CONTROLLED_ACTION         = blocked
```

## Invariants preserved

- ONE_FORENSIC_FSM
- PASS_IS_LOCAL
- PASS_IS_PREREQUISITE_ONLY
- NO_PASS_INHERITANCE
- UNKNOWN_IS_NOT_PASS
- DEFAULT_DENY
- OWN_GATE_EVIDENCE_REQUIRED
- FRESH_EVIDENCE_REQUIRED_FOR_PROMOTION
- NO_CREDENTIAL_EXPOSURE
- RAW_HASH != SEMANTIC_HASH
- SOURCE_TRUTH_NOT_OVERWRITTEN
- BRAIN_IS_GOVERNANCE_CONTROL_PLANE
- CHAT_IS_INTERFACE_ONLY
- 320 MiB Render guard
- Layer 1 / Room 02 / staircase remain locked

## Next

Remain in external-evidence wait for the Brain gate while allowing Quant-N010 safe local prerequisite work. No self-attestation may change CI_TESTED, RUNTIME_VERIFIED, or PROMOTION.
