# BRAIN-N043 — Dual Verifier CI Gate

## Change

The foundation workflow now executes both deterministic security boundaries:

1. `python tools/verify_foundation.py`
2. `python tools/verify_access_path.py`

This prevents a future maintainer from running only the broad verifier while accidentally omitting the explicit two-key/protected-room boundary.

## CI evidence

Commit: `05030f3614f5f2eed109703c0f3377528b25898a`

GitHub workflow lookup returned **zero workflow runs** for this commit.

Therefore:

```text
CI_STATUS = UNKNOWN
```

not PASS and not FAIL.

## Architectural status

The implementation now explicitly represents:

```text
CORRIDOR KEY
    ↓
ROOM KEY
    ↓
REQUEST / BELL
    ↓
INNER LATCH
    ↓
AUTHORIZED OCCUPANT RELEASE
    ↓
ENTRY
```

For protected rooms there is no direct external path from valid keys to entry.

## Next requirement

Investigate the CI zero-run condition and establish an independent reproducible execution path. Evidence must include exact commit SHA, verifier exit codes, compact output, and memory peak. Do not fabricate evidence.

Promotion: DENY.
Layer 1: LOCKED.
Staircase: LOCKED.
