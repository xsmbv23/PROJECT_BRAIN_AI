# BRAIN-N001 — Brain Foundation

## Objective

Create the independent `Project_Brain_AI` governance repository without mixing Brain into `xsmb-quant`.

## Implemented

- `README.md`
- mandatory future-Bot handoff protocol
- Fosennic foundation document
- communication corridor/capability contract
- compact evidence protocol
- append-only action ledger protocol
- current state and exact next action state files
- Brain security gate
- executable verification self-test
- GitHub Actions runtime verification boundary
- lightweight deny-by-default runtime health server
- lightweight Docker runtime

## Runtime verification

GitHub Actions run `31945737967` completed successfully on commit `ce6d416fd1c19cb80a1f63cbbd8e4d3a3bb57590`.

Verified behavior includes:

- valid registered corridor accepted;
- promotion remains DENY;
- replay denied;
- unknown corridor denied;
- layer mismatch denied;
- missing lineage denied;
- capability scope mismatch denied;
- audit retained;
- no secret values in evidence contract.

The first failed run was itself used diagnostically: the workflow invoked a package module as a script, so repository root was absent from `sys.path`. The workflow was corrected to `python -m verification.selftest` and the next run passed. This is retained as forensic history rather than erased.

## Render

Render is intentionally not connected yet. The runtime container is deliberately lightweight and exposes only a health/governance read boundary with mutation and promotion denied.

## Governance

```text
BRAIN FOUNDATION = RUNTIME_VERIFIED
PROMOTION = DENY
LAYER 1 = LOCKED
CANONICAL XSMB TRUTH = xsmb-quant
```

## Next action

`BRAIN-N002`: connect the lightweight Brain runtime to Render as a read-only health/governance boundary, verify deployment, then register the first explicit XSMB↔Brain evidence corridor. Do not add Layer 1 business intelligence before this foundation gate is closed.
