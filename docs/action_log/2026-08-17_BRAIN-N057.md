# BRAIN-N057 — First Live Runtime Evidence / Memory Guard Hardening

## Render evidence

The repaired detector-boundary HEAD reached a live Render deployment. This proves the boot gate returned zero, because `brain_server.py` starts the HTTP server only after the boot gate succeeds.

Observed Render memory at 2026-08-17 00:59 UTC:

```text
instance 8szhj: 30,978,048 bytes
```

A prior live instance also showed 165,457,920 bytes. Both are well below the 320 MiB conservative verifier guard and the 512 MiB platform boundary.

## Evidence gap found

The boot-gate PASS JSON was not visible in the Render log stream because the previous deployment occurred before the stdout flush change was deployed. The service being live is indirect evidence, but the foundation contract requires explicit compact evidence.

## Corrective hardening

The boot gate now:

- flushes PASS/FAIL evidence;
- parses `tracemalloc_peak_bytes` from the foundation verifier;
- denies startup if memory evidence is missing;
- denies startup if verifier peak >= 320 MiB.

## Governance

```text
RUNTIME EXECUTION = PROVEN
SERVICE LIVE = PROVEN
MEMORY = WELL BELOW GUARD
EXPLICIT BOOT ENVELOPE = PENDING
PROMOTION = DENY
LAYER 1 = LOCKED
STAIRCASE = LOCKED
```

## Next

`BRAIN-N058` — deploy the memory-guard/flush hardening and capture explicit `runtime_boot_gate=PASS` plus verifier memory evidence.
