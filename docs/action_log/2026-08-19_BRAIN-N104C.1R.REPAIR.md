# BRAIN-N104C.1R.REPAIR — Exact-Commit Receipt Pin Repair

## Original repair

The earlier N104C.1R deployment failed before transport evidence could run because the action-receipt validator required canonical `last_action_id` / `next_action_id` pointers. That validator drift was repaired.

## Fresh exact-current Render observation

A later Render deployment is now LIVE and its runtime boot gate is PASS:

```text
commit = 2d4415a875df3582aa26df4598f4f409c3c23027
deploy = dep-da2tjk7qj5pc738ht64g
runtime_boot_gate = PASS
foundation tests = 166/166 PASS
tracemalloc peak ≈ 17.4 MB
memory guard = 320 MiB
```

The runtime additionally observed:

```text
DB binding = BOUND_TLS
DB one-time round-trip = PASS
write = PASS
read = PASS
sha_match = true
promotion = DENY
```

These are local gate observations and do not promote source truth.

## Immutable receipt pin

The existing immutable receipt is:

```text
path = evidence/receipts/N104C.1_action_receipt.json
action_id = BRAIN-N104C.1
receipt commit = 36a1504594d58ed516bbe3ba0d71d81326d69003
```

Exact-current runtime:

```text
2d4415a875df3582aa26df4598f4f409c3c23027
```

Therefore:

```text
RECEIPT_COMMIT == RUNTIME_COMMIT = FALSE
ACTION_RECEIPT_GATE              = DENY
N104C.1 TRANSPORT                 = NOT RUN
CANONICAL QUORUM                  = DENY
TRUTH ADMISSION                   = DENY
LAYER 1                           = LOCKED
STAIRCASE                         = LOCKED
```

## Critical invariant

`evidence/receipts/N104C.1_action_receipt.json` MUST NOT be rewritten to follow the later runtime commit.

A later Git commit does not inherit an earlier immutable action receipt.

The new contract `contracts/runtime_commit_receipt_pinning_v1.json` formalizes this rule.

## Why the system is not “failed”

This is not a scraper failure and not a database failure. It is a **receipt-to-runtime identity denial**.

```text
RUNTIME HEALTHY
      !=
ORIGINAL ACTION RECEIPT VALID FOR RUNTIME
```

That distinction is now part of the Forensic admission model.

## Next action

`BRAIN-N104C.1R.REPAIR` remains READY to establish a fresh action-specific receipt for the repair itself, bound to the exact runtime commit that executes that repair. That receipt may never substitute for the original N104C.1 receipt.

Only after a fresh repair receipt is proven may a separately identified transport action be opened.
