# BRAIN-N064 — Pragmatic Ground-Truth Admission Semantics

## Decision

The N01-N100 chain remains active and mandatory under Pragmatic Ground-Truth.

Pragmatic Ground-Truth is an admission strategy, not a replacement for the forensic foundation.

## Required interpretation

- N01-N040 validate doctrine and prevent circular validation.
- N041-N070 validate state authority and forensic FSM behavior.
- N071-N090 validate runtime trust and state-drift conditions.
- N091-N100 validate source identity and FULL27 completeness/shape.
- Excel may become the ground-truth anchor only after its own admission gate passes.
- `EXCEL_VS_WEB_MATCH` is a local comparison result and cannot inherit or grant unrelated PASS states.
- Canonical quorum remains a separate gate.

## Critical correction to successor language

Do not say simply: `Ground-Truth replaces N103`.

Correct:

`Pragmatic Ground-Truth replaces the original network-origin independence proof strategy at the specific canonical-quorum gate, while preserving N01-N100 and every independent prerequisite.`

## Chain

```text
N01-N040
   -> doctrine
N041-N070
   -> state authority / FSM
N071-N090
   -> runtime trust / drift
N091-N100
   -> source identity / FULL27
EXCEL_GROUND_TRUTH_ADMISSION
   -> WEB FETCH
   -> EXCEL_VS_WEB_MATCH
   -> CANONICAL_QUORUM
   -> downstream admission
```

Every transition is local. No PASS inheritance.

## Forensic invariant

`DB_EXISTS`, `DB_BINDING`, `DB_TLS_ADMISSION`, `DB_ROUND_TRIP`, `EXCEL_GROUND_TRUTH_ADMISSION`, `EXCEL_VS_WEB_MATCH`, and `CANONICAL_QUORUM` are distinct gates. A PASS in one never implies PASS in another.

## Status

N064 is a doctrine/documentation closure. No Layer 1 unlock occurs.
