# BRAIN-N016 — Durable Evidence Sink

## Result
IMPLEMENTED / RUNTIME WIRING PENDING

## What was verified

Render workspace `tea-d9fnck6rnols73cbumj0` was explicitly authorized.
An existing Render Postgres instance is available:

- `xsmb-runtime-db`
- id `dpg-da07g0ou01pc738i3ldg-a`
- Singapore
- Free plan
- PostgreSQL 18

No second evidence authority was created.

## Runtime adapter

Added `xsmb-quant/verification/durable_evidence_sink.py`.

The adapter:

- uses `DATABASE_URL` only;
- forces `sslmode=require` when absent;
- creates only the compact `forensic_evidence` table;
- writes evidence envelopes idempotently by evidence SHA;
- reads by evidence SHA;
- fails closed when `DATABASE_URL` is missing or evidence cannot be read.

## Connector observation

The Render SQL connector reached the existing database but returned `SSL/TLS required` when attempting the read-only schema inspection. This is recorded as an integration limitation, not a database health PASS/FAIL claim.

## Critical non-claim

The runtime has NOT yet proven durable write/read-back across redeploy. `DATABASE_URL` is not exposed to the automation layer, and no secret/credential was guessed or fabricated.

## Invariants

- FULL_27 remains source truth.
- TAIL_27 remains derived.
- source_count=1 remains quorum-denied.
- evidence remains compact.
- Render 512MB hard ceiling remains.
- 320MB guard remains.
- promotion remains DENY.
- Layer 1 and staircase remain LOCKED.

## Next

BRAIN-N017: wire the existing Render Postgres into the service execution boundary using Render-native service linkage/secret injection, then execute write -> redeploy -> read-back -> SHA comparison. Do not put database credentials in Git.
