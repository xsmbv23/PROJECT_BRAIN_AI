# BRAIN-N056 — Runtime Credential-Detector Boundary Failure / Repair

## Runtime evidence

N056 again executed the real Render boot gate. The anti-drift verifier passed and all 46 unit tests passed.

## Failure

The source scanner then flagged `core/credential_guard.py` itself.

That module intentionally contains the signatures it is designed to detect. This is the same class of false positive as the earlier verifier self-match, but at a security-detector boundary.

## Repair

`source_scan()` now explicitly excludes only:

- its own verifier implementation;
- `core/credential_guard.py`, whose signatures are separately covered by the credential-guard unit tests;
- `tests/` adversarial fixtures.

No broad security-directory exclusion was introduced.

## Governance

```text
ANTI-DRIFT = PASS
UNIT TESTS = 46/46 PASS
SOURCE SCAN = FAIL (detector self-signature) → REPAIRED
RUNTIME BOOT = DENY
PROMOTION = DENY
LAYER 1 = LOCKED
STAIRCASE = LOCKED
```

## Next

`BRAIN-N057` — redeploy the narrow detector-boundary repair and capture the complete boot-gate outcome. If all gates pass, immediately measure Render memory and durability evidence before any closure-readiness decision.
