# BRAIN-N108 — Transport Probe Execution Boundary

## Verdict

`N108 = VALID BUT NOT EXECUTABLE (CURRENT TOOL SURFACE)`.

This is a forensic-valid DENY, not a logic failure.

## Exact current facts

- Forensic FSM is frozen.
- PASS semantics are local to each gate.
- PASS never inherits into a deeper gate.
- `tools/transport_probe.py` is the required unchanged primitive.
- Local execution is forbidden.
- Proxy evidence is forbidden.
- Source inspection is not runtime proof.
- Fake/replayed receipts are forbidden.
- The current agent tool surface has no authorized primitive for executing an arbitrary command inside the already-live Render container.

## Why execution is blocked

The system has a correct execution requirement but lacks an authorized execution channel in the current control surface.

Therefore:

```text
LOGIC                 = CORRECT
FORENSIC INTEGRITY    = PRESERVED
EXECUTION FEASIBILITY = BLOCKED
TRANSPORT EXECUTION   = NOT_EXECUTED
TRANSPORT RECEIPT     = NOT_PROVEN
PROMOTION             = DENY
```

## Forbidden workarounds

Never unblock N108 by:

1. running the probe on a local machine;
2. using curl/proxy from outside the exact Render runtime;
3. reading HTML/source and treating it as runtime evidence;
4. fabricating a TransportReceipt;
5. replaying an old receipt;
6. modifying `transport_probe.py` to produce a passing result;
7. adding a new endpoint whose implementation changes the probe semantics;
8. treating source code inspection as execution evidence.

## Two admissible operational paths

### Path A — External operator execution

An authorized operator executes the unchanged probe inside the exact live Render container using an approved runtime shell/command mechanism and returns the compact receipt.

### Path B — Capability-only execution bridge

If an execution bridge is introduced, it may only trigger the existing unchanged probe. It must not alter probe logic, transport semantics, receipt fields, source truth, or promotion rules.

The bridge itself is not evidence. The receipt produced by the exact runtime execution is evidence.

## Receipt requirements

The resulting receipt must be compact and contain only the existing approved fields:

- action_id
- url
- status_code
- bytes_read
- sha256
- truncated
- elapsed_ms
- verdict

It must be cryptographically bound to the exact runtime identity.

## Completion gate

N108 cannot close PASS until:

```text
exact live Render runtime
    -> unchanged transport_probe.py
    -> real execution
    -> compact receipt
    -> runtime identity binding
    -> receipt accepted by Forensic FSM
```

Until then the immutable truth remains `NOT_EXECUTED / NOT_PROVEN / DENY`.

## Successor handoff

Do not keep searching for code changes after the capability boundary has been proven absent. The next action is an operational capability acquisition, not another forensic-logic rewrite.
