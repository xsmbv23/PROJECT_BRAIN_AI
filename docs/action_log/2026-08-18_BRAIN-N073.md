# BRAIN-N073 — Edge / EV<0 Doctrine Preservation

## Trigger

User instruction explicitly reinforces autonomous next-action authority while requiring Forensic invariants and Edge discovery / EV<0 handling.

## Action

Added `docs/forensic/EDGE_EV_NEGATIVE_DOCTRINE.md`.

## Result

The foundation now explicitly distinguishes:

```text
EDGE_UNKNOWN  != EDGE_PRESENT
EV_UNKNOWN    != EV>=0
EV < 0        != EV_UNKNOWN
```

Both `EV_UNKNOWN` and `EV < 0` deny the governed action, but they are different forensic states:

- `EV_UNKNOWN` = insufficient admissible evidence.
- `EV < 0` = admissible evidence explicitly indicates negative expected value.

Brain is forbidden from calculating or manufacturing Edge/EV. Quant Engine / Layer 1 owns the calculation when legitimately unlocked. Brain owns admission, evidence preservation, routing, and state transitions.

## Edge handling

No current external Quant Engine event was independently observable through the connected GitHub surface, so no Edge/EV value was invented and no canonical input was admitted.

Current state remains:

```text
FOUNDATION = FROZEN
ACTION_SPACE = 0
PROMOTION = DENY
ROOM_02 = LOCKED
STAIRCASE = LOCKED
```

This is a correct governed hold, not a failure.

## Successor handoff

Next autonomous action is permitted only when a fresh external event becomes independently observable. When it does, the first classification must preserve the distinction between `EDGE_UNKNOWN`, `EDGE_PRESENT`, `EV_UNKNOWN`, `EV<0`, and `EV>=0` before any downstream gate is considered.
