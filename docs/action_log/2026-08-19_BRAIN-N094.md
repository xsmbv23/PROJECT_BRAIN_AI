# BRAIN-N094 — Canonical Source Quorum

## Assessment

N094 began from the correct invariant: Room 01 currently has one source receipt and requires two independent sources. Canonical promotion therefore remains DENY.

The existing Room 01 receipt for `2026-08-12` is explicitly tail-only:

```text
source_file = Ket_Qua_Loto27.xlsx
full27_count = 27
stored values = TAIL_27
source_count = 1
quorum_required = 2
canonical_eligibility = DENY_QUORUM_LT_2
```

This receipt is not sufficient to compare two `FULL_27` source vectors. Therefore the system must not pretend that a second source agreement can be established from tail-only evidence.

## Source-B implementation

The data plane now registers `https://www.xsmb.com.vn` as independent `publisher_e` and contains:

```text
xsmbv23/xsmb-quant/data/ingestion/xsmb_source_b.py
xsmbv23/xsmb-quant/scripts/collect_source_b_receipt.py
xsmbv23/xsmb-quant/.github/workflows/source-b-receipt.yml
```

The collector is bounded-memory by design:

- raw HTML is streamed to disk while hashing incrementally;
- only an 8 MiB parse window is retained in RAM;
- the target date block is isolated before parsing;
- FULL_27 counts are strict;
- TAIL_27 is derived only from FULL_27;
- source-B candidate promotion is hard-coded DENY until quorum is proven.

`ketqua16.net` remains registered as an independent source and its advertising/navigation/scripts are excluded from canonical extraction by policy.

## Current blocker

The source-B collector can now produce a real candidate receipt, but the existing Source-A Room 01 receipt does not retain the canonical FULL_27 vector. It retains only TAIL_27 and hashes.

Therefore:

```text
SOURCE_B = IMPLEMENTED
SOURCE_B_LIVE_RECEIPT = NOT_YET_OBSERVED
SOURCE_A_FULL27 = NOT_PROVEN
FULL27_QUORUM = NOT_PROVEN
CANONICAL = DENY
RESEARCH = LOCKED
ROOM_02 = LOCKED
STAIRCASE = LOCKED
```

This is an intentional forensic stop, not a failure.

## Next action

`BRAIN-N095` — recover or re-materialize the real Source-A `FULL_27` evidence for `2026-08-12` from the data plane, preserving provenance and without using the legacy tail-only workbook as canonical truth. Only after Source-A FULL_27 is available may the live Source-B receipt be compared and quorum evaluated.
