# BRAIN-N085 — Exact-Current Deployment Verification

## Trigger

N084 hardened the state-artifact verifier and explicitly required exact-current runtime verification before any state transition. Render later reported deployment `dep-da22s4lbedkc73d89kq0` as `live`.

## Exact-current deployment evidence

Service:

```text
project-brain-ai
srv-da0506u1egvs73ftsdng
```

Deployment:

```text
dep-da22s4lbedkc73d89kq0
```

Commit:

```text
eae5fc2c09c54cc2b13902cdf8d92843d4ca0097
```

Render status:

```text
live
```

The deployment API reports `finishedAt=2026-08-18T10:05:18.57042Z` and the exact-current Render app log reports `Your service is live 🎉` at `2026-08-18T10:05:18.853361288Z`.

## Runtime gate evidence

Exact-current boot log:

```text
runtime_boot_gate = PASS
commit_sha = eae5fc2c09c54cc2b13902cdf8d92843d4ca0097
memory_guard_bytes = 335544320
```

State-consistency gate:

```text
status = PASS
last_action_id = BRAIN-N083_WAIT_EXTERNAL_EVENT
next_action_id = BRAIN-N083_WAIT_EXTERNAL_EVENT
promotion = DENY
layer_1 = LOCKED
staircase = LOCKED
action_space = 0
```

Foundation gate:

```text
tests_ok = true
gate = PASS
source_scan = PASS
adapter_import_scan = PASS
in_memory_audit_round_trip = PASS
durable_db_round_trip = NOT_PROVEN
brain_governance = PASS
room_lock = PASS
corridor_sensor = PASS
inner_latch = PASS
security_chain = PASS
database_binding_contract = PASS
tracemalloc_peak_bytes = 14024345
```

The test runner reports:

```text
93 tests
OK
```

Access-path gate:

```text
status = PASS
protected_room = OWNER_ROOM
state = RELEASED
```

Database admission contract:

```text
status = PASS
contract_id = RENDER_DB_BINDING_V1
ordered_gates = DB_EXISTENCE -> DB_BINDING -> DB_TLS_ADMISSION -> DB_ROUND_TRIP -> PROMOTION
rule = PASS_AT_GATE_IS_PREREQUISITE_ONLY; NEVER_INFER_DEEPER_PASS
unknown_is_not_pass = true
promotion_requires = BOUND_TLS + COMPACT_WRITE_READ + SHA256_MATCH
```

Deterministic replay:

```text
status = PASS
replay = PASS
contract_hash = 84c0ad581343556dba75a813401d3b8618b060ed96ab5fc44c8e99f3c773205b
contract_hash_repeat = 84c0ad581343556dba75a813401d3b8618b060ed96ab5fc44c8e99f3c773205b
mutation = NONE
external_event_manufactured = false
room_02_unlocked = false
staircase_unlocked = false
```

Database binding probe:

```text
bound = true
tls = true
status = BOUND_TLS
```

## Critical gate: Network Origin Proof

The exact-current runtime explicitly reports:

```text
network_origin_proof = NOT_PROVEN
network_reason = NETWORK_ORIGIN_PROBE_FAILED:OperationalError
network_admission_probe = DISABLED
```

The DB round-trip action is consequently:

```text
db_round_trip_action = DENY
write_read_hash_match = false
promotion = DENY
mutation = NONE
```

This is the correct result. `BOUND_TLS` is a local admission result only. It does not inherit `NETWORK_ORIGIN_PROOF`, and network failure does not become PASS merely because the database binding exists.

## N085 decision

```text
DEPLOYMENT_EXACT_CURRENT = PASS
STATE_CONSISTENCY         = PASS
FOUNDATION                = PASS
ACCESS_PATH               = PASS
DATABASE_BINDING_TLS      = PASS
NETWORK_ORIGIN_PROOF      = NOT_PROVEN
DB_ROUND_TRIP              = NOT_PROVEN
PROMOTION                 = HARD DENY
ACTION_SPACE               = 0
MANDATORY_NO_OP            = ON
LAYER_1                    = LOCKED
STAIRCASE                  = LOCKED
```

## Forensic interpretation

N085 proves the N084 repair is actually running in the exact-current deployment. It also proves that the system preserves gate locality:

```text
DB_BINDING PASS
      !=
NETWORK_ORIGIN PASS
      !=
DB_ROUND_TRIP PASS
      !=
PROMOTION PASS
```

This is one Forensic admission chain, not multiple independent Forensic systems.

## Next action

`BRAIN-N086` — wait for and monitor the declared external network-origin event. No self-manufactured network event, no inferred PASS, no credential mutation, no Layer 1 unlock, and no staircase unlock. Allowed work while waiting is limited to exact-current evidence inspection and non-mutating forensic documentation/integrity checks.
