# BRAIN-N061 — Exact-Current Runtime Evidence

## Result

The repaired deployment is LIVE:

- commit: `2db244e5379914b80667c83ec4f1d03b468f1fee`
- deploy: `dep-da16apk9egvs73amc7s0`
- runtime boot gate: PASS
- foundation verifier: PASS
- access-path verifier: PASS
- tests: 50 / 50
- foundation tracemalloc peak: 901,376 bytes
- observed Render memory: 32,600,064 bytes peak in the measured startup window
- conservative Render guard: 335,544,320 bytes (320 MiB)
- instances: 1
- WEB_CONCURRENCY: 1

## Forensic conclusion

N061 proves the repaired exact-current runtime is healthy and materially below the 512 MB Render Free boundary.

N061 does **not** prove a durable PostgreSQL evidence round-trip.

The exact-current Render logs contain no observable invocation of `tools/durable_postgres.py` and no durable evidence write/read receipt. The Render Postgres instance itself is available, but the service-side credential binding is not observable through the safe connector surface. No credential or connection string was fabricated, exposed, or written into the repository.

Therefore:

```text
RUNTIME BOOT              = PASS
FOUNDATION TESTS          = PASS
ACCESS PATH               = PASS
OOM SAFETY                = PASS
DURABLE DB ROUND-TRIP     = NOT PROVEN
PROMOTION                 = DENY
LAYER 1                   = LOCKED
STAIRCASE                 = LOCKED
```

## Architectural decision

Do not move PostgreSQL code into `core/` merely to obtain a green test. The durable adapter remains in `tools/`, lazy-loads `psycopg`, requires TLS, hashes compact envelopes, and fails closed.

The next work is specifically a **credential/binding boundary problem**, not a reason to weaken Forensic controls.

## Next action

`BRAIN-N062` — establish a safe Render-to-service database binding path that does not expose credentials, does not store credentials in GitHub, preserves TLS-only policy, and emits a compact non-secret round-trip receipt. If the available control surface cannot establish that binding safely, keep the durable DB gate DENY and document the exact external binding requirement.
