# QUANT-N001 — Layer 1 Temporal / Domain / Density / Replay Hardening

## Authority boundary

Brain foundation remains frozen. This action is executed in `xsmbv23/Quant_Engine` only.

`Project_Brain_AI` remains the governance/persistent-memory control plane. `Quant_Engine` remains Layer 1 execution. No Brain infrastructure gate was reopened.

## Implemented

### 1. First-class TIME INDEX

Added `time_index_contract.py` and `DayRecord(date, values)`.

T-1/T-2/T-7 resolve by calendar date:

```text
anchor_date - 1 calendar day
anchor_date - 2 calendar days
anchor_date - 7 calendar days
```

Record index is forbidden as temporal identity.

### 2. MISSING DAY SEMANTICS

Versioned policies:

- `STRICT` — default, missing calendar day => hard deny.
- `GAP_AWARE` — explicit measurement mode; missing observation remains missing and is never synthetically filled.

### 3. NUMERIC DOMAIN LOCK

Canonical daily input:

```text
integer
0..99
exactly 27 values/day
```

Wrong cardinality or out-of-domain values => hard deny.

### 4. FEATURE DENSITY GUARD

Density is measured over the 100-value universe.

Default threshold:

```text
0.10
```

Default action:

```text
WARNING
```

A DENY action requires explicit threshold/policy; no hidden threshold becomes a trading decision.

### 5. REPLAY MODE

Added:

```text
python replay.py receipt.json
```

Only allow-listed room versions execute. Replay compares canonical feature snapshot and output hashes. Same input + same room version/signature must reproduce identical results.

## Current Room

`room_01_signal_v4.py`

Status:

```text
DATE_ALIGNED
DOMAIN_LOCKED
DENSITY_GUARDED
REPLAYABLE
DETERMINISTIC
```

V3 remains historical evidence and is not overwritten.

## Verification added

`tests/test_time_index_and_room_v4.py` covers:

- exact 27-value domain
- 0..99 domain
- strict missing-day deny
- gap-aware no-fill semantics
- calendar-date T-2 resolution across a gap
- explicit density warning
- deterministic replay PASS

## Forensic invariant

```text
PASS IS PREREQUISITE ONLY.
PASS DOES NOT INHERIT FORWARD.
UNKNOWN IS NOT PASS.
TEMPORAL FEATURES ARE DATE-ALIGNED.
TEMPORAL DATA IS NEVER FABRICATED.
OUT-OF-DOMAIN IS HARD DENY.
REPLAY IS FIRST-CLASS.
```

## Resource safety

No Render deployment was required for this Layer 1 repository action. Brain foundation remains dataset-free and the Render 512 MB / 320 MiB guard remains unchanged.

## Result

```text
QUANT-N001 = COMPLETE
BRAIN FOUNDATION = FROZEN
LAYER 1 = HARDENED
ROOM 01 = V4
STAIRCASE = LOCKED
```

## Next

`QUANT-N002` — real-source measurement/replay audit using the canonical dated input, with temporal correctness and no-lookahead evidence. Do not introduce predictive probability or trading decisions in Room 01.
