# REALITY-N010 — Transport-Only Discipline and Receipt-2 Stop

## Normative rule

There is one Forensic system. Database admission gates are sequential states in one chain, not independent forensic systems.

The same non-inheritance rule applies to Reality observation:

```text
RECEIPT_1 = ROUNDTRIP_VALID (TRANSPORT ONLY)
        |
        v
RECEIPT_2
        |
        v
TRANSPORT COMPARISON ONLY
        |
        v
STOP
```

`SO SÁNH != HIỂU` is immutable.

## N010 allowed evidence

Only these fields may be compared:

- http_status
- content_type
- final_url
- raw_byte_count
- sha256
- observed_at

No HTML parsing, 27-field extraction, normalization, domain mapping, canonical schema, business-date inference, cross-source merge, or domain-truth claim is allowed at N010.

## Execution attempt

A current web observation of `https://ketqua16.net/` was available through an external web retrieval surface. The page visibly contains lottery-result content and navigation, but that retrieval surface does not expose the exact raw response bytes and complete transport headers required to construct a forensic RECEIPT_2.

A direct container-side raw HTTP attempt was also blocked by the execution environment's DNS/network boundary. Therefore no raw byte hash was fabricated.

## Decision

```text
RECEIPT_2 = NOT_CAPTURED
N010      = NOT_COMPLETE
PROMOTION = DENY
PARSING   = FORBIDDEN
INFERENCE = FORBIDDEN
```

This is a correct Forensic stop. A visible webpage is not equivalent to an immutable raw transport receipt.

## Important source observation

The external observation currently shows `ketqua16.net` responding as a lottery-results site and containing navigation/advertising-related links. This is useful for source inventory only; it is NOT Receipt-2 evidence and must not be promoted into source truth.

## Next action

`REALITY-N010-RECEIPT-2` remains READY.

The next implementation must obtain RECEIPT_2 from the controlled runtime/source-capture path with exact raw bytes and transport metadata, then persist the compact receipt and stop at transport comparison.
