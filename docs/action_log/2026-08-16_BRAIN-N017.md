# BRAIN-N017 — Durable Evidence Roundtrip Gate

## Action

Added `xsmb-quant/verification/durable_evidence_roundtrip.py`.

The verifier performs:

```text
compact envelope
  -> local evidence SHA recomputation
  -> PostgreSQL persist
  -> PostgreSQL read-back
  -> evidence SHA comparison
```

Bulk source data is never loaded by the roundtrip verifier.

## Runtime result

The existing Render Postgres authority is available in DATA workspace:
`dpg-da07g0ou01pc738i3ldg-a`.

A direct read-only connector attempt was rejected because the database requires SSL/TLS. The code adapter already enforces `sslmode=require` when `DATABASE_URL` is present.

The Render web service currently does not expose a verified `DATABASE_URL` linkage through the available control surface. An attempted environment mutation was blocked by the platform safety layer, so no secret or connection string was guessed, fabricated, or written to GitHub.

Therefore:

`DURABILITY_RUNTIME = PENDING/DENY`

This is intentional fail-closed behavior.

## Invariants

- existing Postgres remains the sole intended evidence authority;
- no competing database created;
- no credentials committed;
- no fabricated persistence evidence;
- 512MB hard boundary;
- 320MB conservative memory guard;
- FULL_27 remains source truth;
- TAIL_27 remains derived;
- quorum 1/2 remains DENY;
- promotion remains DENY;
- Layer 1 remains LOCKED;
- staircase remains LOCKED.

## Next action

BRAIN-N018: establish a verified Render-native linkage from `xsmb-quant` to the existing Postgres authority, then execute the roundtrip. If linkage cannot be established without exposing credentials or creating a competing authority, remain DENY and document the boundary.
