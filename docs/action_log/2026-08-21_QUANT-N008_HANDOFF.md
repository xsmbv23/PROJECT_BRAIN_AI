# QUANT-N008 HANDOFF — Brain/Quant Boundary

## Why this exists

Brain N125 is currently waiting for independently observable exact-current CI/governance evidence. That wait is real and must not be bypassed.

The Brain state explicitly permits safe parallel Quant work as a local prerequisite only. Quant-N008 therefore proceeds without changing Brain action space, promotion, Room 02, or staircase state.

## Work completed in Quant Engine

`xsmbv23/Quant_Engine` completed QUANT-N008:

- source-specific contract for `ketqua16.net`
- source-specific contract for `xsmb.com.vn`
- explicit `OFFICIAL_RESULT_PANEL_ONLY` requirement
- advertising/navigation/header/footer classified as `NON_TRUTH_CONTENT`
- generic page-number regex forbidden as truth extraction
- raw SHA and semantic SHA kept semantically distinct
- 27-value canonical domain required
- ambiguous/missing/duplicate/out-of-domain semantic extraction denied
- provenance fields required
- promotion remains forbidden in Room 01
- structural verifier added without network I/O

Quant current state remains a read-only projection and cannot override Brain authority.

## Permanent Forensic lesson

The existence of a contract is not proof that its implementation obeys the contract.

Therefore every source contract must eventually progress through:

```text
DEFINED
→ IMPLEMENTED
→ TESTED
→ RUNTIME_VERIFIED
→ CROSS_REPO_VERIFIED
→ FORENSIC_ADMITTED
→ PROMOTED
```

No state may inherit PASS from the previous state.

## Security / data truth

Advertisements are not truth content. A page can change bytes because of advertising while retaining the same canonical result. Therefore raw-byte identity and semantic agreement must remain separate evidence channels.

## Brain status after handoff

```text
CI exact-current observation = UNKNOWN
promotion                    = DENY
action_space                 = 0
Room 02                      = LOCKED
staircase                    = LOCKED
```

This handoff does not alter those states.
