# BRAIN-N054 — Runtime Foundation Failure Chain / Hardening

## Render evidence

The N054 deployment reached the real boot gate and produced two deterministic foundation failures.

### Failure A — Brain policy schema pin

The integrated Brain test failed because `PolicyPin("v1.0")` compared the request's `v1.2` schema as an exact string, while the Brain admission contract explicitly validates schema by **major version**.

Corrective action:

- policy version remains exact;
- Brain state version remains exact;
- schema compatibility is now major-version based;
- a v1.x request is admissible under a v1.0 pin;
- v2.x remains denied.

### Failure B — Source scanner self-match

The credential-like source scanner detected its own regex literal in `tools/verify_foundation.py`.

Corrective action: construct scanner literals from fragments so the scanner does not match its own detection pattern while retaining detection of actual credential-like strings.

### Additional repair discipline

The previous schema-fix write temporarily truncated `core/foundation_hardening.py`. This was immediately detected by comparing the prior blob and restored in full before continuing. The complete AuditChain, schema-major validator and quarantine primitives are now preserved.

## Security decision

No gate was bypassed.

```text
ANTI-DRIFT = PASS
RUNTIME BOOT GATE = DENY
FOUNDATION = FAIL → REPAIRED
PROMOTION = DENY
LAYER 1 = LOCKED
STAIRCASE = LOCKED
```

## Next

`BRAIN-N055` — redeploy the repaired schema/scanner HEAD and capture the next complete gate result, then measure Render memory before considering closure readiness.
