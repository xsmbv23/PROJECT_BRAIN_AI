# BRAIN-N111 — Epistemic Succession DNA

## Scope

This is a doctrine hardening action. It does **not** replace or promote the active runtime action N109.

## Permanent separation

```text
DOCTRINE != EVIDENCE != STATE != HISTORY != HYPOTHESIS
```

The fifth category, `HYPOTHESIS`, is now explicitly protected because Quant systems are especially vulnerable to converting plausible ideas into apparently proven edges.

## Non-inheritance rules

```text
HISTORY PASS      != CURRENT PASS
DOCTRINE          != CURRENT PASS
HYPOTHESIS        != EDGE
EDGE              != EV+
```

A successor Bot must verify every transition with evidence owned by the relevant gate.

## Hypothesis promotion

```text
HYPOTHESIS
  -> CONTROLLED TEST
  -> REPLAY
  -> OOS
  -> STABILITY
  -> EDGE
  -> PROBABILITY
  -> PAYOUT/COST
  -> EV
```

Any missing prerequisite is not PASS. `EV < 0` is DENY.

## Succession principle

> A successor Bot inherits the right and duty to verify; it does not inherit the right to trust.

## Fossilization protection

Implementation is replaceable. Doctrine is versioned. Evidence is attributable. State is current-only. History is immutable. Hypotheses remain provisional until promoted by evidence.

This prevents both:

1. cargo-cult succession; and
2. architectural fossilization.

## Active execution gate

N109 remains the sole admitted action. N111 does not close N109 and does not unlock Room 01, Layer 1, or the staircase.

```text
N109 = READY_BUT_EXTERNAL_CAPABILITY_BLOCKED
ROOM_01 = LOCKED
LAYER_1 = LOCKED
STAIRCASE = LOCKED
```
