# BRAIN-N064 — Successor Inheritance Freeze

## Decision

No new executable transition is authorized at this moment.

The canonical runtime state is:

```text
STATE        = WAIT_EXTERNAL_EVENT
MODE         = ACTIVE_HOLD
REAL_EVENT   = NOT_OBSERVED
EVIDENCE     = NONE
TRANSITION   = NONE
AUTHORITY    = NONE
ACTION_SPACE = 0
ACTION       = NO-OP
```

## Why

The current `state/current_state.json` is explicitly sealed and defines one Forensic FSM with local PASS semantics. The current `state/next_action.json` is also explicitly `WAIT_EXTERNAL_EVENT` and says that absence of a real GitHub Actions dispatch requires NO-OP.

This is not inactivity. It is an intentional terminal hold for the current phase.

## Frozen laws

```text
NO_PASS_INHERITANCE
UNKNOWN != PASS
DEFAULT = DENY
EVIDENCE -> STATE -> ACTION
```

There is no legal transition of the form:

```text
STATE -> ACTION
```

and no legal transition:

```text
PASS(Gate N) -> PASS(Gate N+1)
```

A PASS is local to the gate that produced its evidence. It is only a prerequisite for evaluating the next gate.

## Database admission semantics

There is ONE Forensic FSM.

```text
DB_EXISTENCE
   -> DB_BINDING
      -> DB_TLS_ADMISSION
         -> DB_ROUND_TRIP
            -> PROMOTION
```

These are gates, not independent Forensic states.

`DB_EXISTS=PASS` never means `DB_ACCESS=PASS`.
`DB_BINDING=PASS` never means `DB_ROUND_TRIP=PASS`.

## External-event boundary

The only authorized transition source for the current phase is:

```text
REAL_GITHUB_WORKFLOW_DISPATCH
    -> fresh runtime receipt
    -> EVIDENCE > 0
    -> TRANSITION AUTHORITY
```

Do not manufacture, simulate, self-trigger solely to bypass the boundary, reuse historical receipts, or create alternate paths around the hold.

## Successor instruction

This action log is part of the durable inheritance chain. A future Bot must read `state/current_state.json`, `state/next_action.json`, and this document before taking action.

If no real external event has occurred, the correct action remains:

```text
NO-OP
```
