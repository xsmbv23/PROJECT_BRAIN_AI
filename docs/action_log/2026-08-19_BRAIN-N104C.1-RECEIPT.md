# BRAIN-N104C.1 — Action Receipt Provenance

## Exact observation basis

The exact-current Render deployment observed before receipt persistence was:

- commit: `36a1504594d58ed516bbe3ba0d71d81326d69003`
- deploy: `dep-da2s267lk1mc73conitg`
- status: `live`
- runtime boot gate: `PASS`
- runtime log admission gate: `DENY` on missing N104C.1 receipt
- database binding: `BOUND_TLS`
- database round trip: `PASS`
- write/read SHA: `4df18b811c8ceef01aadbaa6395c20f03e53425be98036e4bb88a31772b1c821`

The runtime health body hash `2f60e1e7ec1d0aa15c431ce5ba7712a4e8dcd83c5b24882130f4742c209389` is independently reproducible from the exact `brain/server.py` and `brain/__init__.py` at commit `36a1504594d58ed516bbe3ba0d71d81326d69003`, using the runtime-observed commit and `BOUND_TLS` classification.

## Receipt

`evidence/receipts/N104C.1_action_receipt.json` records:

- action `BRAIN-N104C.1`
- observed runtime commit `36a1504594d58ed516bbe3ba0d71d81326d69003`
- HTTP 200
- `/health`
- body SHA256 above
- status `PASS`
- canonical receipt SHA256 `0aa14a78ab9c61bfa60db1d46ebaeb004b5e44b1952061a9c6cd677b3b3f4baa`

## Critical state-drift rule

This receipt is evidence of the **observed runtime**. It must not be interpreted as proof that a later repository commit is the same live runtime.

Because Render is configured with `autoDeploy=yes`, persisting the receipt to `main` may create a new repository HEAD and therefore a future runtime identity. The action-receipt gate must remain strict:

```text
PASS_IS_LOCAL_TO_GATE
NO_PASS_INHERITANCE
RUNTIME_IDENTITY_IS_OBSERVED
GITHUB_HEAD != CURRENT_LIVE_RUNTIME
UNKNOWN_IS_NOT_PASS
DEFAULT_DENY
```

Do not redeploy merely to make the receipt green. If the live runtime changes to a new commit, the old receipt must be treated as historical/local evidence and a fresh exact-current runtime observation is required.

## Current decision

`N104C.1 ACTION RECEIPT = PERSISTED`

`N104C.1 TRANSPORT = LOCKED`

`PROMOTION = DENY`

The transport probe is not unlocked by repository presence alone.
