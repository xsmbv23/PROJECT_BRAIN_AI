# BRAIN-N011 — Bounded Fixture Runtime Evidence

## Execution

The canonical bounded verifier was executed against the real source artifact identified for the 12/08/2026 fixture.

Execution boundary:

`LOCAL_VERIFICATION_RUNTIME`

This is runtime evidence for the bounded chain. It is explicitly **not** a Render capacity measurement.

## Results

- source SHA verified: PASS
- FULL_27 count: 27
- FULL_27 semantic structure: PASS (`10×5, 10×4, 3×3, 4×2`)
- TAIL_27 count: 27
- TAIL_27 derivation from FULL_27: PASS
- source quorum: FAIL (`1 < 2`)
- canonical: DENY
- promotion: DENY

Observed local max RSS was 120104 KB (~117.3 MiB) while reading the single workbook fixture. This must not be extrapolated to Render Free.

## Important correction

The existing fixture's declared payload hash used an older canonicalization. The runtime verifier established a new deterministic canonical payload SHA:

`1f089aa05e6d71ad186ad6aa35bb27858f3571b69da7381e872839c27c9061f2`

This is recorded as runtime evidence rather than silently rewriting the historical evidence record.

## Governance

The runtime execution proves mechanics, not production canonical eligibility.

Because quorum is one source only:

`canonical = DENY`

`promotion = DENY`

Layer 1 remains LOCKED.
Staircase remains LOCKED.

## Next

BRAIN-N012: make the bounded verifier execution reproducible inside the existing Build Plane / Render-safe boundary, with explicit memory guard and no historical aggregation.
