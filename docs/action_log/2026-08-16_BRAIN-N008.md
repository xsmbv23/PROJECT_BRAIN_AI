# BRAIN-N008 — Runtime Footprint and OOM Guard Verification

## Objective

Continue the foundation without allowing the Render Free services to become unbounded compute hosts.

## Render verification

Workspace: DATA's workspace.

Services observed:

- `project-brain-ai` — Render Free, Oregon, Python, 1 instance.
- `xsmb-quant` — Render Free, Singapore, Docker, 1 instance.

Recent Render metrics showed the Brain process at roughly 26–29 MB memory and the XSMB service at roughly 13–16 MB during the observed window. These are observations, not a reservation of the remaining RAM.

Recent Brain logs showed successful build and live deployment, with `WEB_CONCURRENCY=1` and the service live. No OOM event was observed in the sampled logs.

## Governance correction

Observed low memory usage does NOT mean the remaining memory may be consumed by arbitrary jobs.

The 512 MB limit remains an architectural boundary.

Added:

- `docs/EXECUTION_ROUTING_AND_OOM_GUARD_V1.md`
- `Quant_Engine/docs/ROOM_RESOURCE_GUARD_V1.md`

## Required routing

```text
TASK
  |
  +-- P0 -> Brain/UI
  +-- P1 -> bounded execution
  +-- P2 -> external worker/build plane
```

Unknown memory footprint is P2 / DENY on Brain.

## Runtime evidence status

Render deployment is live, but the bounded XSMB end-to-end evidence chain has NOT been claimed as executed by this action.

## Fosennic invariants

- source truth immutable;
- no full-history load into Brain;
- no silent rewrite after crash;
- evidence references artifacts rather than copying raw data;
- promotion remains DENY;
- Layer 1 remains LOCKED;
- staircase remains LOCKED.

## Next action

`BRAIN-N009`: inspect/verify the bounded fixture implementation and create the smallest executable end-to-end test contract without loading the historical dataset.
