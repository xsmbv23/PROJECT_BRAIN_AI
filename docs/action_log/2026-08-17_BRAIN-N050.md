# BRAIN-N050 — State Anti-Drift Hardening

## Finding

`state/current_state.json` was stale at N034/N035 while `state/next_action.json` had advanced to N049. This is a foundation-level continuity risk because successor Bots could follow contradictory pointers.

## Corrective action

Created:

```text
 tools/check_state_consistency.py
```

It verifies:

- `current_state.next_action_id == next_action.action_id`;
- promotion remains DENY;
- Layer 1 remains LOCKED;
- staircase remains LOCKED;
- CI status is explicit and non-fabricated.

The check was wired into `.github/workflows/foundation.yml` before the integrated and focused security verifiers.

## Static evidence

Workflow source and verifier source were inspected after the change. The anti-drift gate is present before the security gates.

## Runtime evidence

Still UNKNOWN. Current agent container cannot reach `github.com`, and the GitHub connector exposes no workflow run for the preceding commit. No PASS is inferred.

## Governance

```text
PROMOTION = DENY
LAYER 1 = LOCKED
STAIRCASE = LOCKED
RUNTIME VERIFICATION = UNKNOWN
```

## Next

BRAIN-N051 — acquire real runtime evidence for the current HEAD, with anti-drift gate included.
