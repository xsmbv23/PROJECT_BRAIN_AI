# BRAIN-N068 — ACTIVE HOLD / IMMUTABLE FAILURE EVIDENCE

## Canonical status

```text
ACTION SPACE = 0
MODE         = ACTIVE_HOLD
EXECUTION    = MANDATORY_NO_OP
PROMOTION    = DENY
ROOM 01      = LOCKED
ROOM 02      = LOCKED
STAIRCASE    = LOCKED
FORENSIC     = INVARIANT
```

## External failure evidence — immutable

GitHub Actions Run ID:

`32098912473`

Run number:

`90`

Repository:

`xsmbv23/Quant_Engine`

Workflow:

`.github/workflows/quant-engine-tests.yml`

Job ID:

`95595513698`

Conclusion:

`failure`

Observed result:

- 82 tests
- 2 failures
- 3 errors

Exact checked-out commit:

`1d379507d09ab99686d7f44ff5910f141a758383`

This failure is forensic evidence and must never be overwritten, deleted, or reinterpreted as a PASS.

## Repair target

Required fresh validation commit:

`1fa007eefa43b68b636347a513a5749ced90853a`

Repair commits recorded before it:

- `f7b065e2236d8ee69c73b5bb97c72411d7c32908`
- `7fd7d01dde96517e87bfa1c4f587384dd11570b7`
- `1d6e24b0501b190f6c95812767cac178960946ed`
- `1fa007eefa43b68b636347a513a5749ced90853a`

## Repair facts already established

- ISO date normalization removes mixed `datetime.date` / string ambiguity in the forensic envelope.
- XSMB fixture alignment is restored to the full 27-value prize domain; no 4-value synthetic substitute is accepted.
- Receipt assertions respect the execution-signature schema and do not move fields merely to satisfy tests.
- Ketqua16 HTTP responses are explicitly closed to avoid resource leakage / side effects.

## Critical separation

```text
REPAIR COMMIT != TEST RECEIPT
OLD FAILURE  != NEW RESULT
RUN NUMBER   != RUN ID
RUN ID       != CONCLUSION
```

A new GitHub Actions run is a new external event. Brain may observe and verify it but may not manufacture it.

## N068 completion gate

The hold may end only after a fresh GitHub Actions run:

1. uses workflow `.github/workflows/quant-engine-tests.yml`;
2. checks out exactly `1fa007eefa43b68b636347a513a5749ced90853a`;
3. completes successfully;
4. exposes the exact Run ID, Job result, conclusion, and valid receipt independently;
5. passes the applicable Forensic gate without reusing the old failure as success evidence.

Until all conditions are independently verified:

```text
NO-OP
NO WORKAROUND
NO ALTERNATE PATH
NO RECEIPT FABRICATION
NO HISTORICAL RECEIPT REUSE
NO STAIRCASE UNLOCK
NO PROMOTION
```

## Successor handoff

Read, in order:

```text
AI_START_HERE
  -> governance
  -> docs/forensic/FORENSIC_ADMISSION_CHAIN.md
  -> state/current_state.json
  -> state/next_action.json
  -> this action log
```

The permanent doctrine is:

> Evidence -> State -> Action.

And:

> PASS is local to its gate; PASS is only a prerequisite; FAIL is immutable evidence; UNKNOWN is not PASS.
