# BRAIN-N040 — Unified Security-Chain Verifier Integration

## Implemented

`tools/verify_foundation.py` now explicitly imports and exercises `core.security_chain.evaluate_entry_request`.

The deterministic verifier therefore checks not only isolated security modules but their ordered composition.

## Required invariant

```text
OBSERVE
  -> ROOM AUTHORIZATION
  -> REQUEST INNER RELEASE
  -> ASSERT INNER RELEASE
  -> ENTRY
```

A protected-room request with valid external credentials must stop at `RINGING` / `INNER_LATCH_SECURED` until an authorized occupant releases it.

## CI observation

GitHub workflow-run lookup for the latest state commit returned zero workflow runs. CI is therefore informational and not evidence of PASS.

No PASS claim is made.

## State

- Promotion: DENY
- Layer 1: LOCKED
- Staircase: LOCKED
- Render memory boundary: 512 MB hard / 320 MB guard

## Next

BRAIN-N041: reconcile CI and obtain reproducible verifier evidence before closure readiness.
