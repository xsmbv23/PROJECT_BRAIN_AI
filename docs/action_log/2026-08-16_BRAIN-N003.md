# BRAIN-N003 — Architecture and Memory Boundary Correction

## Trigger

The project owner required a full reread of the uploaded architecture/context and clarified that the chat window is only a communication channel.

## Source review

The full available master-context document was read through its final line. The review reconfirmed:

- source truth versus Quant representation is a hard boundary;
- FULL_PRIZE must preserve provenance;
- LEGACY_TAIL27 must never be relabeled FULL_PRIZE;
- same-date replacement requires validation/provenance/quorum/contract/forensic handling;
- data repair is not a silent historical rewrite;
- manifest is forensic output, not a disposable cache;
- Render restart must not retrain/reoptimize/rewrite history;
- performance constraints must not override forensic integrity;
- the original UI/deployment contract is a protected boundary;
- future Bots need persistent handover outside the chat.

## Architecture correction

Added:

- `docs/ARCHITECTURE_MAP_V1.md`
- `docs/CHAT_BOUNDARY_AND_MEMORY.md`
- `docs/STAIRCASE_TO_L1.md`

These documents explicitly define:

```text
CHAT = COMMUNICATION INTERFACE ONLY

PERSISTENT REPOSITORY / EVIDENCE / STATE
= MEMORY + GOVERNANCE + CONTINUITY
```

## State correction

`state/current_state.json` was corrected so that Brain security runtime verification is not confused with Render deployment verification.

Current state:

```text
Brain security       = RUNTIME VERIFIED
GitHub self-test     = PASS
Render current code  = UNVERIFIED
Promotion            = DENY
Staircase            = LOCKED
Layer 1              = LOCKED
```

## New continuation

`state/next_action.json` now points to `BRAIN-N004`.

BRAIN-N004 must verify the Render boundary and then register/map the Data Foundation corridors, including FULL_PRIZE provenance, LEGACY_TAIL27 preservation, canonical TAIL_27 derivation, evidence envelopes, shards/manifests and OOM-safe projections.

## Important

No Layer 1 code is authorized by this action.
