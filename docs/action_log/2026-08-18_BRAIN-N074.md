# BRAIN-N074 — FSM Runtime Reconciliation / Replay Verification Handoff

## Exact current runtime evidence

Render deployment:

`dep-da20f6m1egvs73942020`

Commit:

`c28c0a579d9a96739b427e6527e1d76668ac02da`

Observed runtime boot gate:

```text
PASS
```

Observed foundation:

```text
73 tests
0 failures
tracemalloc_peak_bytes = 1,049,277
```

Observed admission FSM verifier:

```text
status = PASS
states = 5
external_event_path = ISOLATED
foundation_path = ADVANCE_ALLOWED_EXTERNAL_STATE_UNCHANGED
room_02 = LOCKED
promotion = DENY_UNTIL_FRESH_EVIDENCE
```

Observed database state remains:

```text
binding = DENY_TLS
tls admission = DENY
round trip = NOT_PROVEN
promotion = DENY
```

The foundation test suite reporting `durable_round_trip = PASS` is not promoted to durable DB truth because the exact-current runtime binding probe independently reports `DENY_TLS`. This is a deliberate application of pass locality and no evidence inheritance.

## External Event Path

N070 remains untouched:

```text
WAIT_EXTERNAL_EVENT
ACTION_SPACE = 0
MANDATORY_NO_OP
NO_SELF_MANUFACTURED_EVENT
NO_ALTERNATE_PATH
```

## Foundation Path result

FSM contract is persisted and enforced at boot. The Foundation Path may advance without changing the External Event Path.

## Runtime pointer-drift finding

A later exact-current deployment attempted to boot with a persistent pointer mismatch:

```text
current_state.next_action_id = BRAIN-N074
next_action.action_id       = BRAIN-N075
```

The runtime correctly failed closed with:

```text
next_action mismatch: current=BRAIN-N074 state=BRAIN-N075
```

This is a **Forensic state-pointer drift**, not a domain-data event and not evidence of DB admission.

The authoritative successor pointer is `state/next_action.json`. Therefore N074 closes with the canonical state transition:

```text
last_action_id = BRAIN-N074
next_action_id = BRAIN-N075
next_brain_action = BRAIN-N075
```

No promotion, Layer 1, staircase, DB, or external-event state is changed by this reconciliation.

## Invariant

```text
current_state.next_action_id == next_action.action_id
```

Mismatch => `FOUNDATION_BOOT_GATE_DENY`.

This invariant is now part of the successor doctrine: a Bot may never execute an action different from the persistent successor pointer.

## Next

`BRAIN-N075` — independently replay and verify the one-chain Forensic FSM, prove PASS locality/no PASS inheritance, and preserve the external-event path without manufacturing an event.
