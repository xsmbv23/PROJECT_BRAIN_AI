# BRAIN-N015b — Compact Evidence Envelope Implementation

Implemented `xsmb-quant/verification/evidence_envelope.py`.

The envelope is deterministic and metadata-only. It records source/fixture/FULL_27/TAIL_27/manifest hashes, status, promotion, resource status, peak RSS and elapsed time, then derives an evidence SHA from canonical JSON.

Forbidden bulk categories include raw payload, workbook, historical rows/dataframes, all-results collections and source bytes.

This is an implementation boundary only. Durable persistence and restart verification remain pending until runtime evidence proves them.

Promotion remains DENY; Layer 1 and staircase remain LOCKED; 512MB hard ceiling and 320MB conservative guard remain mandatory.
